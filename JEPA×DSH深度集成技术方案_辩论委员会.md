# JEPA × DeepSeek Harness（DSH）深度集成技术方案

> 产出方式：四位 AI 架构专家组成的辩论委员会，经六轮严格辩论收敛。
> 参与者：**架构师（Architect）**、**JEPA 专家（JEPA-Specialist）**、**DSH 专家（DSH-Specialist）**、**质疑者（Skeptic）**。
> 日期：2026-08-18 ｜ 底座：本目录《JEPA持续学习框架调研与设计方案.md》（CL-JEPA 最小核心）。

---

## 0. 关键约束与假设速览（辩论的锚点）

> 所有假设在此显式标注，后续方案不得绕过。编号沿用全文。

| # | 约束/假设 | 内容 | 来源 |
|---|---|---|---|
| C1 | 参数预算 | 20 GB 指**推理期显存预算**。JEPA 世界模型按 V-JEPA 2 规模（~1.2B，bf16≈2.4 GB），LLM 编解码器 ≤3B（int8≈3 GB），各类 head/码本 <0.5 GB，激活+缓冲 <6 GB，总峰值 <12 GB，留 8 GB 冗余 | 用户约束 |
| C2 | 激活参数量级 | 每推理步**激活 ~1–3B**：走世界模型时激活 JEPA 1.2B；走文本 I/O 时激活 LLM ≤3B。二者不在同一条热路径同时激活 | 推导 |
| C3 | JEPA 能力边界 | JEPA 不生成文本、无零样本指令遵循、无开放域文本生成；只能做**表征空间预测 + 潜空间规划** | 用户约束 + JEPA 谱系 |
| C4 | DSH 工程约束 | 一切皆插件；主循环原为"文本进→文本出"；有插件生命周期、轨迹日志、版本回滚 | 用户约束 |
| C5 | 观察模态 | Agent 的"世界"= 屏幕/应用窗口画面 + 结构化 JSON 观察。JEPA 的视觉预训练表征**不能直接复用**到纯文本观察，需多模态状态编码器 | 推导 |
| C6 | 决策延迟预算 | 单决策步额外开销 ≤ 1.5 s（Agent 场景下工具调用/API 本身耗时数秒，1 s 内规划开销可忽略） | 推导 |
| C7 | 训练实时性 | 在线更新**不阻塞**主循环；默认冻结主干，仅 head + 码本 + 目标映射器在线更新，LoRA 残差微调为受控例外 | 用户强调 |
| A1 | 未验证假设 | 文本观察 → JEPA 潜空间的跨模态对齐质量、潜空间 MPC 在离散工具环境下的 rollout 稳定性，需 MVP 阶段实测 | 明示 |
| A2 | 可选改动 | 预测器 MoE 稀疏化、可学习可塑性门控（meta-learned plasticity）为**可选二期**，非 MVP 必需 | 明示 |

---

# 第一部分：六轮辩论记录

---

## 第 1 轮：总体架构与数据流

**议题**：JEPA 的插件定位；连续表征如何适配 DSH 文本数据流；是否引入"表征总线"；状态/动作/目标如何表示。

**架构师（陈述）**
JEPA 不该塞进 DSH 现有的"模型插件"槽——那个槽是"文本进文本出"的 LLM 契约，JEPA 满足不了（C3）。也不该只当"记忆插件"，那会把它阉割成特征提取器。**结论是：新增一个 `JEPA-Loop` 插件，作为全新的 Agent 主循环**，LLM 从"决策中枢"降级为"文本编解码器（Codec）+ 降级备援"。数据流上，DSH 原本是单总线（文本），现在要**双总线**：一条文本总线维持插件生态兼容，一条潜空间总线承载 JEPA 的连续表征；LLM 是两总线之间**唯一**的翻译器，避免出现 N×M 的对齐爆炸。

**JEPA 专家（陈述）**
定位为世界模型 + 规划器，符合 LeCun 的蓝图——世界模型本来就该是"不说话的"（language 是独立模块）。关键约束我强调三点：(1) 预测发生在**潜空间**，所以状态必须是潜向量；(2) JEPA 的预测器（V-JEPA 2 里是 ViT-S ~22M）是轻量的，**潜空间 rollout 极便宜**，这是规划可行性的根基；(3) 世界模型预测的是"状态转移"，与模态无关——只要状态编码器把任何观察都压成一个好的潜向量，预测器不需要知道原始输入是像素还是 JSON。

**DSH 专家（陈述）**
工程上我接受双总线，但有一个硬要求：**新插件必须走 DSH 的插件生命周期与轨迹日志**，否则版本回滚、可观测性全废。我提议把"潜状态"作为一个一等字段写进轨迹日志（每条轨迹同时存 `obs_text` 和 `state_latent`），这样第 6 轮的离线采集就有落点。表征总线要做成**版本化键值存储**，不是黑箱内存。

**质疑者（三问）**

> **Q1** 你们说 JEPA 是 Loop 插件，但 DSH 的 Loop 插件接口是"文本进文本出"。一个不生成文本的模块当 Loop，接口签名是什么？总不能用"返回空字符串 + 副写内存"这种 hack 吧？
> **Q2** "双总线 + LLM 唯一翻译器"——这听起来很优雅，但谁负责保证两套空间的**语义对齐**？潜向量 512 维和文本 token 空间根本没有天然对应，翻译器自己也是要训练的，这等于把最难的问题推迟到了"翻译器"上。
> **Q3** 状态在表征空间怎么定义？DSH 的现实观察大多是 JSON/文本，JEPA 是拿视频预训练的（V-JEPA 2 是 100 万小时视频），那个视觉表征对"JSON 状态"基本没用。你们的世界模型到底在预测什么？会不会变成"在没人定义好的空间里预测一个没人定义好的东西"？

**回应与修正**

- 对 Q1：**Loop 接口改为"潜状态进 → 潜状态出"**，新增 `ILatentLoop` 接口（`tick(state_latent, goal_latent) -> (action_latent, confidence)`）。原 `ITextLoop` 接口保留，由 `JEPA-Loop` 内部持有一个 **LLM Codec** 完成潜→文本的边界转换。这不是 hack，是把"编解码"显式变成一个可替换插件。
- 对 Q2：**承认 Q2 是本质困难**，修正方案：不追求"全空间对齐"，而是**只对齐三个离散锚点**——动作（码本）、目标（原型）、状态（编码器）。三者都用**离散码本/原型**作为跨空间的地标，而非连续向量互映射。这正好落在用户一贯坚持的"离散标签联觉"路线上：文本侧和潜空间侧共同锚定在离散符号上，LLM 只在锚点之间做结构化生成。
- 对 Q3：**承认 JEPA 视觉预训练不能直接复用**，修正：引入**多模态状态编码器**——视觉分支（复用 V-JEPA 2 冻结骨干）处理画面，文本分支（小 LLM 编码器）处理 JSON 观察，二者拼接到统一潜向量 `s∈ℝ^512`。世界模型预测的是**这个统一潜空间里的 `s_t → s_{t+1}` 转移**（`ŝ_{t+1} = g_φ(s_t, a_t)`），与底层模态解耦。JEPA 的价值不在"认像素"，而在**在潜空间预测状态转移、并用预测误差当世界模型的校准信号**——这正是 V-JEPA 2-AC 已经证明可行的范式（仅 62 小时机器人数据就让动作条件预测可用）。

**本轮结论**
1. JEPA = 新 `JEPA-Loop` 插件（潜空间主循环），LLM = Codec + 降级备援。
2. 引入**双总线**：文本总线（兼容层）+ 潜空间总线（决策层），LLM 是唯一翻译器。
3. 跨空间对齐**只做离散锚点对齐**（动作码本 / 目标原型 / 状态编码器），不做连续向量互映射。
4. 状态 = 多模态编码器产出的统一潜向量；世界模型 = 模态无关的潜空间前向模型。

---

## 第 2 轮：工具调用与动作对齐

**议题**：动作潜向量 → 可执行工具调用；动作码本构建；参数解码；新插件零重训扩展。

**JEPA 专家（陈述）**
actor（动作头）输出一个连续动作潜 `a∈ℝ^64`，我们用一个**动作码本**（VQ，K=512 个码）把它量化成离散码，每个码绑定一个工具 + 一个"参数槽位模板"。这是 JEPA 能给的极限——它只能输出连续向量，**结构化文本（参数 JSON）必须交给 LLM Codec**。所以动作对齐分两段：`连续向量 →(最近邻) 离散工具码`，`离散工具码 + 状态上下文 →(LLM) 参数 JSON`。

**架构师（陈述）**
码本要有三层结构：**簇（Cluster，语义族，如"检索类/写入类/网络类"）→ 码（Code，具体工具）→ 参数模板（Schema）**。这样新工具进来，先在簇内做软检索，码本容量压力被分散。

**DSH 专家（陈述）**
新工具如何不重训就进码本：**工具嵌入 = LLM 对"工具名 + docstring + 参数 schema"的文本编码**，投影到码本空间（ℝ^64）。新插件加入 → 生成一个新码点，无需重训，只需更新码本索引。但这里有个致命前提，见下面质疑。

**质疑者（三问）**

> **Q1** 码本 K=512，但 DSH 插件是**无限增长**的。几百个工具就顶满码本了，你们要么无限扩码本（违背有限内存），要么淘汰旧码（灾难性遗忘），怎么解？
> **Q2** 说"新工具零重训扩展"是**幻觉**。actor 从没在"工具嵌入空间"里训练过，它输出的 64 维向量跟新工具的嵌入根本不在一个几何邻域里，最近邻检索等于随机选。零样本扩展是假的，除非 actor 被显式训练成"指向工具嵌入空间"。
> **Q3** 参数怎么从 64 维向量解码？字符串参数、枚举值、嵌套 JSON 对象，怎么可能从连续向量**回归**出来？任何声称"连续回归参数"的方案在工程上都是灾难。

**回应与修正**

- 对 Q1：**分层 + 软淘汰**。码本分两层：外层"簇码本"（~32 个语义簇，稳定），内层"工具码"（每簇内最近邻，容量用**频率衰减 + 原型合并**管理，类似 DCA 的 `outcome_archetypes` 上限合并策略）。冷门工具码降权而非硬删，保留"待唤醒"态。
- 对 Q2：**承认，这是本轮最重要修正**。改为：actor 头的训练目标里**加入对比项**，让 `a` 与"该步真实所用工具的嵌入"拉近、与其余工具推远（InfoNCE）。这样 actor 学会的是"在工具嵌入空间里指位置"，而非自由连续向量。新工具加入时，用**少样本校准**（几十条示例，冻结主干，只微调 actor 头 + 一个轻量投影）做上下文校准，而不是靠"纯最近邻"赌泛化。
- 对 Q3：**彻底放弃连续回归参数**。连续向量只回答两个离散问题：**选哪个工具、哪些槽位需要填**（注意力/倾向）。参数 JSON 的**具体值由 LLM Codec 生成**（输入 = 工具 schema + 槽位倾向 + 状态上下文）。这守住了 JEPA 的边界（不生成文本），又把结构化生成交给本就擅长此事的 LLM。

**本轮结论**
1. 动作对齐三段式：`actor → 连续动作潜 →(最近邻) 工具码 →(LLM) 参数 JSON`。
2. 动作码本分两层：稳定簇层 + 可增长工具层，冷门码软淘汰。
3. actor 头用 InfoNCE 训练，使其输出**指向工具嵌入空间**；新工具靠"少样本校准"而非纯最近邻。
4. 参数解码**不连续回归**，由 LLM 在槽位倾向引导下做结构化生成。

---

## 第 3 轮：目标编码与任务理解

**议题**：目标（NL/示例/结构化）→ 目标潜向量；目标映射器训练；零样本新任务；降级与澄清。

**架构师（陈述）**
目标映射器是"用户意图 → 潜空间"的入口。三种目标源要归一：自然语言走 LLM 编码 → 投影；示例（few-shot 演示）走状态编码器编码演示轨迹的终态；结构化描述直接拼接成文本再走同一条文本路径。统一出口是一个目标潜向量 `g∈ℝ^256` + 一个置信度标量。

**JEPA 专家（陈述）**
目标映射器的训练信号是**对齐**：`(目标描述, 成功轨迹的终态潜)` 应为正对，`(目标描述, 无关终态)` 为负对，InfoNCE。这个数据**天然来自 DSH 轨迹日志**——每条轨迹都有用户目标 + 最终状态，是免费的监督。零样本新任务靠 LLM 文本编码器的泛化 + 投影层的仿射插值。

**DSH 专家（陈述）**
冷启动问题：一开始没有轨迹，映射器没得训。方案是**LLM 先当规划器跑**（影子模式），一边跑一边积累 `(goal, outcome)` 对，攒够再离线训映射器。这跟 V-JEPA 2 的"两阶段"（先无监督、后少量适配）同构。

**质疑者（三问）**

> **Q1** 目标映射器训练数据要"任务执行成功一次"才有一个 `(goal, outcome)` 对，这是**先有鸡还是先有蛋**：没有映射器就没有目标编码，没有目标编码就执行不了任务，执行不了任务就没有数据。死锁。
> **Q2** 零样本新目标：投影层 `文本嵌入 → 目标潜` 只在训练分布内有效。一个完全没见过的任务（比如"帮我把邮件归档并按发件人聚类"），LLM 能理解，但投影层会把它投到一个**任意方向**，目标潜是 OOD 垃圾。
> **Q3** "无法编码就降级/澄清"——你**怎么检测**"无法编码"？潜空间里没有一个天然的"这目标我懂不懂"的仪表盘。给不出检测标准，降级就是一句空话。

**回应与修正**

- 对 Q1：**打破死锁靠"LLM 影子规划"**。冷启动期 JEPA 目标映射器不参与，LLM 用文本目标直接规划并执行（这正是 DSH 现有能力），轨迹日志持续采集 `(goal_text, outcome_latent)`。这是"先用贵的方法收集数据、再用便宜的方法替换"的经典 bootstrapping，死锁被 LLM 的既有能力解开。数据量门槛：约 500–2000 条多样化成功轨迹即可离线训出可用投影层（对比学习的数据效率）。
- 对 Q2：**用原型库兜底 + 不迷信投影层**。目标潜不直接等于投影层输出，而是 `g = 投影(文本) + 检索修正`：在已有目标原型库里做 kNN，若近邻原型"任务语义相近"，用原型插值校正投影；若最近原型距离也很大（OOD），进入 Q3 的降级路径。投影层只学"文本空间到潜空间的粗仿射"，精细定位靠原型。
- 对 Q3：**给出可计算的检测标准**：`conf = -min_dist(g, 目标原型库)`（即最近原型距离取负，越大越懂）。配套阈值 `τ_goal`：`conf < τ_goal` → 降级。降级链分两级——先由 LLM Codec 生成澄清问题（"你是指 A 还是 B？"），用户回答后重编码；若仍低，则**回退 LLM 直接规划**（本步放弃 JEPA，纯 LLM 兜底），同时把这条"没编码好的目标"记入日志供离线改进映射器。

**本轮结论**
1. 目标映射器 = 多源归一 → LLM 编码 → 投影 → `g + conf`。
2. 训练数据来自 DSH 轨迹日志的 `(goal, outcome)` 对，冷启动由 LLM 影子规划 bootstrapping 解开死锁。
3. 零样本靠"投影粗仿射 + 原型插值校正"，不迷信单点投影。
4. 降级检测 = 最近原型距离阈值；降级链 = 澄清 → 回退 LLM 规划。

---

## 第 4 轮：规划与决策循环

**议题**：规划算法；探索/利用；长时程分层；失败检测与重规划。

**JEPA 专家（陈述）**
在潜空间做规划有天然优势：预测器轻（ViT-S ~22M），**rollout 极便宜**。我主张 **latent MPC（模型预测控制）** 而非 MCTS：采样一批动作序列，用世界模型滚出未来潜状态，用 cost 模块打分，选最优首动作。H-JEPA 的启示是**分层**——低层做短时程精细预测，高层做长时程抽象规划；长时程任务在"概念空间"里生成子目标，再把子目标喂回低层 MPC。

**架构师（陈述）**
探索/利用：JEPA 没有 reward，只有预测误差。这反而是**内在动机的天然来源**——预测不确定度越高，越值得去（探索红利）；cost 模块（世界模型里 LeCun 的"cost/critic"）给利用项。形式化为 UCB：`score = 预测代价 + κ · 预测不确定度`。

**DSH 专家（陈述）**
失败检测必须有工程信号，不能只在潜空间里猜。我给三条可观测信号：预测误差持续超阈、cost 单调上升、实际状态与预期轨迹的潜距离发散。任一触发 → 重规划。

**质疑者（三问）**

> **Q1** 潜空间 MPC 便宜但**短视**（只能滚有限步）；MCTS 长视但**贵**。在 20GB 预算、1.5s 延迟上限（C6）下，到底能滚多少步、采多少个候选？给不出数字就是空谈。
> **Q2** 探索/利用的 UCB 里那个 `κ` 和"预测不确定度"从哪来？世界模型是确定性前向网络，**哪来的不确定度**？用一个拍脑袋的 σ 就敢叫"探索红利"？
> **Q3** 长时程子目标"在概念空间生成"——概念空间本身怎么来？谁来定义概念？这又是个"先有鸡还是先有蛋"，除非你们说清概念空间如何涌现。

**回应与修正**

- 对 Q1：**给数字**。单次潜空间 rollout（1 步预测器前向，ViT-S 22M）≈ 0.5–2 ms。预算 800 ms 规划 → 可做 **K=32 候选 × H=20 步 ≈ 640 次前向**（批量后 <800 ms），或 CEM 迭代 4 轮 × 每轮 64 候选 × H=16。这个量级对 Agent 足够（工具调用本身耗时数秒）。状态编码（JEPA backbone 前向）每步仅 1 次 ≈ 100–200 ms，是规划里最贵的一环，所以"多 rollout、少 re-encode"是省钱原则。
- 对 Q2：**不确定度来自集成，不是拍脑袋**。用**多预测器集成**（同构不同初始化，或不同 dropout 种子）——同一 `(s,a)` 的多个预测的方差即不确定度。成本低（预测器才 22M，挂 4–6 个才 ~100M，远在预算内），且不确定度有几何意义。κ 随"任务是否允许冒险"可调，但基线用集成方差，不是 σ 常数。
- 对 Q3：**概念空间 = 高层 H-JEPA 的潜 + 原型聚类**，不是人为定义。做法：高层编码器把低层状态序列压成概念潜，对概念潜做**原型聚类**（复用 DCA `outcome_archetypes` 的"上限合并"策略）得到离散概念符号。子目标 = 概念空间里介于"当前概念"与"目标概念"之间的中间原型。这是"符号从连续潜中涌现"，不需要人工标签。

**本轮结论**
1. 规划 = **latent MPC**（短时程）+ **概念空间子目标**（长时程，H-JEPA 分层）。
2. 探索/利用 = UCB，不确定度来自**预测器集成方差**（非拍脑袋常数）。
3. 失败检测 = 三条可观测信号（误差持续超阈 / cost 上升 / 轨迹潜距离发散）。
4. 概念空间 = 高层潜 + 原型聚类，符号涌现而非人工定义。

---

## 第 5 轮：实时训练与在线适应性（新增核心轮次）

**议题**：在线更新哪些组件、频率、如何不阻塞；快速适应新工具/新任务；灾难性遗忘控制；思考的适应性；延迟与算力预算。

**JEPA 专家（陈述）**
实时性是本方案与"离线预训练一次成型"的分水岭。我的立场分三层：
- **默认冻结**：JEPA backbone（编码器）+ 预测器主干冻结，这是抗遗忘的根本（结构上就忘不了）。
- **在线更新（每步/每 N 步，惊喜门控）**：actor 头、critic 头、目标投影层、动作码本（加性）。更新信号 = 预测误差 E 超过阈值才动（复用 CL-JEPA 的"惊喜门控"：熟悉处零浪费）。
- **受控例外**：当世界动力学真的变了（新环境、新工具族），以 **LoRA 残差**微调编码器/预测器，学习率极低（类比 V-JEPA 2 用 62h 数据就完成动作条件适配——少量微调是可行的），但要触发门槛 + 事后回滚验证。

**DSH 专家（陈述）**
在线更新**绝不能阻塞主循环**。方案：独立 `Online-Trainer` 后台进程，从经验回放缓冲取 batch 算梯度，算完**原子交换** head 权重（写时复制 / double-buffer）。主循环永远读"当前一致版本"，不等梯度。更新频率 = 异步触发（缓冲攒够 N=32 条转移，或 E 连续 K=5 步超阈）。

**质疑者（三问）**

> **Q1** 你说"冻结 backbone 只训 head"，那新工具、新环境带来的**世界动力学变化**（比如换了一个全新 UI 的应用，状态转移规律全变），backbone 永远学不会，这叫什么"适应性"？只训 head 是**自废武功**，等于承认模型无法真正适应新环境。
> **Q2** 灾难性遗忘：EWC 要 Fisher 信息矩阵，在 1.2B 模型上算 Fisher 太贵，等于不落地。你拿什么做遗忘控制？
> **Q3** "思考的适应性"：规划器怎么**感知**自己预测失准并动态调整？这需要把"预测误差"反馈回"搜索深度/探索策略"的闭环，你们有具体机制吗，还是又一句口号？
> **Q4**（追问）在线更新 + 自适应机制，每步**额外计算开销**到底多少？20GB 预算 + 1.5s 延迟下能扛住吗？给数字。

**回应与修正**

- 对 Q1：**承认"只训 head"对世界动力学变化不够，修正为三级可塑性**：
  1. **T1 级（默认，最便宜）**：只更新 head + 码本 + 目标投影。适应"新任务但世界动力学不变"。
  2. **T2 级（触发式，LoRA）**：当预测误差 E 在**新环境下**持续超阈（如连续 100 步 `E > 2×基线`），触发 LoRA 残差微调编码器+预测器，学习率 `lr=1e-5`（主干 `1e-3` 的 1/100），新增参数 <1% 模型。这是真适应新世界动力学。
  3. **T3 级（离线巩固）**：T2 攒下的 LoRA 残差，离线合并/蒸馏进主干（第 6 轮）。
  V-JEPA 2-AC 用 62h 数据完成动作条件适配是 T2 可行性的直接证据。
- 对 Q2：**遗忘控制不用全量 EWC**，用三件更便宜的东西（CL-JEPA 结论）：① **EMA 目标编码器**（O(1) 参数空间回放，天然抗遗忘）；② **原型重放**（只重放 outcome 原型向量，内存恒定）；③ **LoRA 隔离**（T2 微调时主干冻结，残差独立，可整体回滚）。EWC 只对**小 head**（~几十 M 参数，Fisher 可算）使用。这比在大模型上算 Fisher 便宜 2–3 个数量级。
- 对 Q3：**给闭环机制，复用 CL-JEPA 的"预测误差 E = 学习信号 + 资源表"**，E 这一个标量驱动三条自适应杠杆：
  - `E↑ 持续` → **搜索深度↑**（H 从 12 提到 32）、**探索权重 κ↑**（多试不确定动作）、**触发 T2 在线微调**；
  - `E↓ 稳定` → **搜索深度↓**、**启用原型缓存捷径**（状态落入已知原型 ε 邻域直接复用缓存动作，跳过 rollout）、**掩码率↑ / 更新趋零**（省算力）。
  "预测持续失准"的检测 = E 的滑动均值超过历史基线的 2 倍且持续 50 步 → 触发"表征重校准"（先 T2 LoRA，仍无效则回退 LLM 规划 + 记入离线待巩固队列）。
- 对 Q4：**延迟与算力预算表**（RTX 4090 级，bf16）：

  | 阶段 | 操作 | 估计耗时 | 是否阻塞主循环 |
  |---|---|---|---|
  | 状态编码 | JEPA backbone 前向 ×1 | 100–200 ms | 是（必须） |
  | 规划 | 潜 MPC（K=32×H=20，批量） | 200–800 ms | 是 |
  | 动作解码 | 码本最近邻 + LLM 参数生成（短序列） | 50–150 ms | 是 |
  | **在线更新** | head 梯度 1 步（后台） | 5–20 ms | **否（异步）** |
  | **T2 LoRA 微调** | 触发式，后台小 batch | 1–3 s/batch | **否（异步）** |
  | **合计每决策步** | 前四项 | **~0.4–1.2 s** | 满足 C6 |

  关键结论：**在线学习不占用主循环延迟**，因为它是异步的；主循环延迟全部来自"感知 + 规划 + 解码"三项，合计 <1.2s。

**本轮结论**
1. 三级可塑性：T1 默认只训 head/码本/投影 → T2 触发 LoRA 残差微调（新世界动力学）→ T3 离线合并。
2. 遗忘控制 = EMA 目标编码器 + 原型重放 + LoRA 隔离（EWC 只用于小 head）。
3. 思考适应性 = 预测误差 E 驱动三杠杆（搜索深度 / 探索权重 / 在线微调触发）。
4. 在线更新异步、不阻塞；主循环延迟 ~0.4–1.2s，满足预算。

---

## 第 6 轮：离线巩固与整体可行性

**议题**：轨迹日志采集；后思考训练（离线巩固）；20GB 稀疏激活落地；MVP 与整体评估。

**DSH 专家（陈述）**
DSH 的轨迹日志是金矿：每条轨迹天然含 `goal_text + 观察序列 + 动作序列 + 工具结果 + 最终状态`。离线巩固的数据采集 = 给日志**追加潜状态快照字段**（第 1 轮已定），跑一次状态编码器即可回填，无需重新执行任务。

**JEPA 专家（陈述）**
离线巩固（"后思考训练"）做四件事：① **原型蒸馏**——对 outcome 潜做聚类，更新目标原型库与 outcome 原型库；② **码本更新**——对动作潜再聚类，淘汰冷门码、合并近邻码、吸收新工具码；③ **目标映射器重训**——用新增 `(goal, outcome)` 对做对比重训；④ **世界模型精修**——用回放缓冲重训预测器，抑制 rollout 漂移（Clin-JEPA 的五阶段课思想）。这些全部离线、批处理、不碰在线循环。

**架构师（陈述）**
20GB 预算下的稀疏激活：JEPA 是 dense ViT，没有原生 MoE。落地手段三条：**输入级稀疏**（状态编码时只喂显著 token，掩码率随熟悉度↑）；**原型缓存捷径**（命中原型跳过 rollout）；**可选**——预测器换 MoE 层（这是对标准 JEPA 的改动，标记为 A2，非 MVP 必需）。

**质疑者（三问）**

> **Q1** 轨迹日志是"文本 + 工具调用"，不是视频，也不是连续状态。**构造 `(s_t, a_t, s_{t+1})` 潜对时，`s_{t+1}` 是什么？** 工具返回一段文本结果，你怎么把它变成"下一时刻的世界状态潜向量"？
> **Q2** 你们反复说"离线巩固"，但它和"在线更新"**会不会打架**？离线改的码本/原型/主干，在线 loop 正跑着，版本怎么切？DSH 有版本回滚，但**潜空间总线上的东西也要版本化**，否则回滚到一半状态就裂了。
> **Q3** 整体可行性给个准话：这套东西相对"纯 LLM Agent"到底**能不能赢、在哪些指标上赢、会输在哪**？别只画饼。

**回应与修正**

- 对 Q1：**`s_{t+1}` 来自"工具执行后的新观察"，不是工具返回文本本身**。每次工具调用后，Agent 会得到新的观察（新截图/新 JSON），状态编码器把它编码成 `s_{t+1}`。工具返回文本只作为"动作参数是否正确"的**辅助监督**（喂给 LLM Codec 的反馈），不进世界模型的潜空间。若某工具调用后观察不变（如纯计算），则 `s_{t+1}=s_t`，该转移对世界模型贡献为零、自动被惊喜门控忽略——这正是 CL-JEPA"熟悉处零浪费"的体现。
- 对 Q2：**承认，修正为"潜空间总线的版本化 + 两段式切换"**。所有潜侧可写状态（码本、原型库、主干权重、LoRA 残差）都带版本号，与 DSH 的轨迹/插件版本**同源关联**。离线巩固产出一个"新版本快照"，在线 loop 只在**安全边界**（当前任务结束 / 空闲间隙）做**原子切换**，切换失败自动回滚到旧快照。**在线 head 更新与离线巩固的冲突用"离线为主、在线为补"**：离线巩固合并在线攒下的 head 梯度，统一出一个基线，避免两套梯度互相覆盖。
- 对 Q3：**给诚实对比表**。

  | 维度 | 纯 LLM Agent | JEPA×DSH 本方案 |
  |---|---|---|
  | 零样本新任务 | **强**（LLM 指令遵循） | 弱→中（靠原型插值 + 降级回退 LLM） |
  | 开放域文本/闲聊 | **强** | 弱（依赖 LLM Codec） |
  | 长时程规划一致性 | 中（上下文遗忘、幻觉） | **强**（世界模型 + 分层子目标） |
  | 物理/环境动态建模 | 弱（靠提示词硬编码） | **强**（潜空间世界模型） |
  | 样本效率（新工具适应） | 弱（需大量示例/微调） | **中→强**（少样本校准 + LoRA） |
  | 推理延迟 | 低（一次 decode） | 中（+规划开销 ~0.4–1.2s） |
  | 推理成本/步 | 高（大 LLM） | **低**（规划走 22M 预测器，LLM 只做短文本 I/O） |
  | 可解释性 | 低 | 中（潜状态 + 原型可检视） |

  结论：**在"长时程、结构化、可重复、需要世界模型"的 Agent 任务上赢；在"零样本开放域、纯对话、一次性杂活"上输或持平**。因此定位是**"LLM 为主循环的升级版（世界模型增强规划）"，不是"替代 LLM"**——LLM 始终保留降级与 Codec 角色。

**本轮结论**
1. 轨迹日志回填潜状态快照，`(s_t, a_t, s_{t+1})` 的 `s_{t+1}` 来自工具执行后的新观察编码。
2. 离线巩固四件事：原型蒸馏、码本更新、目标映射器重训、世界模型精修；与在线更新"离线为主、在线为补"，版本化原子切换。
3. 20GB 稀疏激活靠输入稀疏 + 原型缓存捷径，预测器 MoE 为可选二期。
4. 定位 = "世界模型增强规划"，LLM 保留 Codec + 降级；在长时程结构化任务上赢，在零样本开放域上回退 LLM。

---

# 第二部分：技术方案文档

---

## 1. 总体架构（文字描述）

### 1.1 新增/修改插件清单

| 插件 | 类型 | 职责 |
|---|---|---|
| `JEPA-Loop` | 新增（Agent Loop） | 潜空间主循环：`感知 → 潜状态 → 规划 → 动作 → 执行 → 更新`，取代 LLM 主循环 |
| `LatentBus` | 新增（基础设施） | 潜空间总线：版本化的潜状态/动作/目标键值存储，双总线之一 |
| `StateEncoder` | 新增 | 多模态观察（画面 + JSON）→ 统一状态潜 `s∈ℝ^512` |
| `GoalMapper` | 新增 | 用户目标（NL/示例/结构化）→ 目标潜 `g∈ℝ^256` + 置信度 |
| `ActionCodebook` | 新增 | 动作码本（两层：簇层 + 工具层），离散码 ↔ 工具绑定 |
| `ActionDecoder` | 新增 | 动作潜 + 工具码 → 结构化工具调用（经 LLM Codec） |
| `Planner` | 新增 | 潜空间 MPC + 概念空间子目标 + UCB 探索/利用调度 |
| `Critic`（Cost） | 新增 | 状态/轨迹代价评估（LeCun 蓝图 cost 模块） |
| `WorldModel`（Predictor） | 新增 | `ŝ_{t+1} = g_φ(s_t, a_t)` 潜空间前向模型（JEPA 核心） |
| `OnlineTrainer` | 新增 | 惊喜门控 + 异步 head 更新 + 遗忘控制 |
| `OfflineConsolidator` | 新增 | 轨迹日志 → 数据 → 原型蒸馏/码本更新/目标映射器重训 |
| `LLM` 插件 | 修改 | 定位改为 "Text Codec + 降级规划器" |
| `TrajectoryLog` 插件 | 修改 | 追加 `state_latent` 快照字段 |

### 1.2 数据流（一个完整任务周期）

```
用户目标(NL/示例/结构化)
        │
        ▼
  ┌ GoalMapper ──────────► g∈ℝ^256, conf
  │  (LLM编码 → 投影 → 原型检索校正)   │ conf<τ → 澄清/回退LLM
        │
        ▼
  ┌ 状态观察(截图/JSON) ──► StateEncoder ──► s∈ℝ^512
  │
  ▼
┌──────────── Planner（潜空间 MPC，循环）────────────┐
│  采样 K 条动作序列 {a_1..a_H}                        │
│  WorldModel rollout: ŝ_{t+1}=g_φ(s_t,a_t)           │
│  Critic 打分 + UCB(集成不确定度)                     │
│  → 选最优首动作 a* ∈ ℝ^64                            │
└───────────────────────────────────────────────────┘
        │ a*
        ▼
  ActionDecoder: a* ──(最近邻)──► 工具码
        │
        ▼
  LLM Codec: 工具码 + schema + 槽位倾向 ──► 参数JSON ──► DSH 执行插件
        │
        ▼
  新观察 ──► StateEncoder ──► s_{t+1}
        │
        ├──► 预测误差 E = ‖ŝ_{t+1} − s_{t+1}‖
        │        ├─ E 高 → 惊喜门控 → OnlineTrainer 异步更新 head
        │        └─ E 持续高 → T2 LoRA 微调 / 回退 LLM
        └──► 轨迹日志追加 (goal, s_t, a, s_{t+1}, outcome)
```

### 1.3 双总线

- **文本总线**：维持 DSH 插件生态兼容，LLM Codec 是唯一出口。
- **潜空间总线（LatentBus）**：承载 `s / a / g / 原型 / 码本`，版本化、原子切换、可回滚。

---

## 2. 核心模块设计

### 2.1 状态编码器 `StateEncoder`

- **输入**：多模态观察 = 屏幕/窗口画面（图像张量）+ 结构化观察（JSON 文本）。
- **结构**：视觉分支 = V-JEPA 2 冻结骨干（输出视觉潜）；文本分支 = 小 LLM 编码器（~0.5B，对 JSON 做均值池化）；拼接后经 2 层 MLP 投影。
- **输出**：`s ∈ ℝ^512`。
- **训练**：视觉分支冻结；文本分支 + 投影层用**跨模态 JEPA 目标**训练（预测"文本观察"与"下一视觉观察"的潜互预测，V-JEPA 2 与 8B LLM 配对做 VideoQA 是可行的证据）；投影层训练数据来自 DSH 轨迹日志的观察对。

### 2.2 目标映射器 `GoalMapper`

- **输入**：目标（自然语言 / 示例轨迹 / 结构化描述）。
- **结构**：归一化 → LLM 文本编码 → 投影层（2 层 MLP，仿射）→ 原型检索校正。
- **输出**：`g ∈ ℝ^256` + `conf = −min_dist(g, 目标原型库)`。
- **训练**：InfoNCE，正对 = `(目标描述, 成功轨迹终态潜)`，负对 = 无关终态；数据来自轨迹日志；冷启动由 LLM 影子规划 bootstrapping。

### 2.3 动作码本 `ActionCodebook`

- **结构**：两层。簇层 `C∈ℝ^{32×64}`（语义族）；工具层 `T∈ℝ^{K×64}`（每工具一码，K 上限 512）。
- **码来源**：工具嵌入 = LLM 对"工具名 + docstring + 参数 schema"的编码，投影到 ℝ^64。
- **更新**：新工具 → 加新码点（无需重训）；冷门码软淘汰；近邻码原型合并（DCA `outcome_archetypes` 策略）。

### 2.4 动作解码器 `ActionDecoder`

- **输入**：动作潜 `a∈ℝ^64` + 状态上下文。
- **流程**：`a →(最近邻) 工具码 → 槽位倾向（attention over schema 槽）→ LLM Codec 生成参数 JSON`。
- **不连续回归参数**：连续向量只决定"选哪个工具 + 填哪些槽"，参数值由 LLM 生成。

### 2.5 规划器 `Planner`

- **短时程**：latent MPC，CEM 或束搜索，K=32、H=20（可随 E 自适应 12↔32）。
- **长时程**：高层概念空间 MCTS 生成子目标 → 喂回低层 MPC（H-JEPA 分层）。
- **探索/利用**：`score = 预测代价 − κ·集成不确定度`；不确定度来自 4–6 个预测器集成方差。

### 2.6 评估器 `Critic`

- **结构**：小 MLP，`V(s)` 或 `Q(s,a)`，训练用 TD(λ) 从轨迹的累计代价回归。
- **代价信号**：任务成功/失败 + 用户反馈 + 硬约束违规（复用 DSH 现有结果信号）。

---

## 3. 工具调用机制

### 3.1 码本构建流程

1. 遍历 DSH 已注册插件，LLM 编码 `(name, docstring, schema)` → 工具嵌入 → 投影 ℝ^64。
2. 离线聚类成簇层（~32 簇），簇内建索引。
3. 新插件注册事件触发增量更新（加码点 + 重建局部索引）。

### 3.2 连续动作 → 工具调用映射算法

```
a* = actor(s, g)                       # ∈ ℝ^64
code = argmin_c ‖proj(a*) − T[c]‖²     # 最近邻
tool = codebook.lookup(code)
slots = attention(a*, schema)          # 槽位倾向
params = LLM_codec(tool, slots, s)     # 结构化参数 JSON
```

### 3.3 参数解码方案

- **枚举/布尔**：从槽位倾向直接取最高概率候选（LLM 在受限解码下完成）。
- **字符串/数值**：LLM Codec 生成，槽位倾向作为软约束注入 prompt（"优先填 `email` 和 `folder` 两个槽"）。
- **嵌套 JSON**：由 LLM 在工具 schema 约束下生成，schema 作为 grammar 约束（DSH 插件本就有 schema 校验）。

---

## 4. 实时训练与适应性机制

### 4.1 在线更新策略

| 组件 | 状态 | 更新频率 | 触发条件 |
|---|---|---|---|
| JEPA backbone（编码器） | **冻结**（T2 时 LoRA） | T2 触发式 | E 持续超阈 |
| 预测器主干 | **冻结**（T2 时 LoRA） | T2 触发式 | 同上 |
| actor 头 | 在线更新 | 每 N=32 转移 | 惊喜门控（E>阈） |
| critic 头 | 在线更新 | 每 N=32 转移 | 同上 |
| 目标投影层 | 在线更新 | 每条成功轨迹 | 任务结束 |
| 动作码本 | 加性更新 | 新工具注册时 | 插件事件 |
| 目标/结果原型库 | 在线 + 离线 | 周期 | 聚类合并 |

### 4.2 快速适应新工具/新任务

- 新工具：LLM 编码生成码点 + **少样本校准**（几十条示例，冻结主干，微调 actor 头 + 轻投影）。
- 新任务：原型插值校正目标潜；失败则降级 LLM 规划并采集数据。
- 元学习/上下文校准：MAML/Reptile 作为**可选二期**（A2），MVP 用"少样本微调 + 原型重放"。

### 4.3 灾难性遗忘控制

1. **EMA 目标编码器** `f_ξ = EMA(f_θ)`：O(1) 参数空间回放，抗遗忘的根本（CL-JEPA 结论）。
2. **原型重放**：重放 outcome 原型向量（固定内存），DER 式钉定。
3. **LoRA 隔离**：T2 微调时主干冻结，残差独立可回滚。
4. **EWC**：仅用于小 head（Fisher 可算），主干不用。

### 4.4 规划器动态调整（思考适应性）

- 预测误差 E 的滑动均值驱动三杠杆：`E↑ → 搜索深度↑ + 探索 κ↑ + 触发 T2`；`E↓ → 搜索深度↓ + 原型缓存捷径 + 更新趋零`。
- "预测持续失准"判定：E 滑动均值 > 2×历史基线 且持续 50 步 → 先 T2 LoRA，仍无效 → 回退 LLM 规划 + 记入离线待巩固队列。

### 4.5 在线学习开销与延迟预算

- 在线 head 更新：异步后台，5–20 ms/步，不占主循环。
- T2 LoRA：异步后台，1–3 s/batch，不占主循环。
- 主循环延迟 = 状态编码(100–200ms) + 规划(200–800ms) + 解码(50–150ms) ≈ **0.4–1.2s**，满足 C6。
- 显存：JEPA 1.2B(bf16 2.4GB) + LLM ≤3B(int8 3GB) + head/码本 <0.5GB + 预测器集成(~100M) + 激活缓冲 <6GB，峰值 <12GB < 20GB。

---

## 5. 离线训练与巩固流程

### 5.1 初始训练数据采集与清洗

- 来源：DSH 轨迹日志（天然含 `goal + 观察 + 动作 + 结果 + 终态`）。
- 采集：对每条轨迹回填 `state_latent` 快照（跑一次 StateEncoder）。
- 清洗：丢弃 E 异常高的噪声转移、重复转移去重、任务类型均衡采样。

### 5.2 在线推理与离线巩固的协作

- 在线攒梯度 → 离线合并为基线（"离线为主、在线为补"）。
- 离线产出新版本快照 → 安全边界原子切换 → 失败自动回滚。

### 5.3 原型蒸馏与抽象表征更新

1. 对 outcome 潜做 k-means（上限 1024，余弦 >0.7 合并）。
2. 更新目标原型库 / 结果原型库。
3. 动作潜再聚类 → 更新码本（淘汰冷门、合并近邻、吸收新工具）。
4. 目标映射器用新增 `(goal, outcome)` 对重训。
5. 预测器用回放缓冲重训（Clin-JEPA 五阶段课抑制 rollout 漂移）。

---

## 6. 可行性分析与风险

### 6.1 工程难度分级

| 难度 | 模块 | 理由 |
|---|---|---|
| 低 | 轨迹日志回填、码本、Critic、原型聚类 | 常规工程 |
| 中 | StateEncoder 跨模态对齐、GoalMapper、latent MPC | 需调对齐与 rollout 稳定性 |
| 高 | 潜空间 MPC 在离散工具环境的 rollout 稳定性、T2 LoRA 触发策略 | 需实验验证（A1） |
| 最高 | 概念空间子目标涌现、跨模态潜对齐质量 | 前沿，可能需降级方案 |

### 6.2 失败模式与缓解

| 失败模式 | 缓解 |
|---|---|
| 潜空间 rollout 发散/漂移 | Clin-JEPA 五阶段课 + 集成不确定度截断 + 短时程优先 |
| 跨模态对齐差（文本↔潜） | 降级 LLM 规划；只做离散锚点对齐不做连续互映射 |
| 新工具最近邻误配 | 少样本校准 + 置信门控 + LLM 兜底 |
| 在线更新与离线巩固冲突 | 离线为主在线为补 + 版本化原子切换 |
| 目标 OOD 编码失败 | conf 阈值 → 澄清 → 回退 LLM |

### 6.3 与纯 LLM 方案对比预期

见第 6 轮 Q3 对比表。核心定位：**世界模型增强规划，非替代 LLM**；在长时程/结构化/需世界模型的任务上赢，零样本开放域回退 LLM。

---

## 7. MVP 实施路线图

| 阶段 | 交付内容 | 验证标准 |
|---|---|---|
| **P0 地基（1–2 周）** | 双总线 + 轨迹日志回填潜状态 + StateEncoder 初版（复用 V-JEPA 2 视觉骨干 + LLM 文本编码） | 观察 → 潜向量可复现；轨迹日志字段完整 |
| **P1 世界模型（2–3 周）** | WorldModel（潜前向模型）+ Critic + 轨迹 → `(s,a,s')` 数据管道 | 预测误差 E 在测试集低于随机基线；rollout 10 步漂移可测 |
| **P2 规划闭环（3–4 周）** | 动作码本 + ActionDecoder + latent MPC + LLM 兜底（影子模式，JEPA 只建议不执行） | JEPA 建议动作与真实动作一致率 > 60%；执行成功率 ≥ LLM 基线 |
| **P3 在线适应性（3–4 周）** | OnlineTrainer（惊喜门控 + 异步 head 更新 + EMA 目标编码器）+ T2 LoRA 触发 | 新工具少样本适应成功率 > 基线；遗忘率（旧任务 BWT）可控 |
| **P4 分层与巩固（4–6 周）** | 概念空间子目标 + OfflineConsolidator（原型蒸馏 + 码本更新 + 目标映射器重训） | 长时程任务成功率 > P2 基线；原型库稳定收敛 |

---

## 附录 A：假设清单汇总

见第 0 节表 C1–C7、A1–A2。核心未验证项：跨模态潜对齐质量、潜空间 MPC 在离散工具环境的 rollout 稳定性——这两项是 P1/P2 的**准入门槛**，若失败则方案降级为"JEPA 只做世界模型异常检测 + LLM 继续规划"的保守形态。

## 附录 B：与既有 CL-JEPA 研究的关系

本方案是《JEPA持续学习框架调研与设计方案.md》的**工程化延伸**：CL-JEPA 最小核心 `{f_θ, f_ξ=EMA(f_θ), g_φ, D}` 直接对应本方案的 `{StateEncoder, EMA 目标编码器, WorldModel, 预测误差 E}`；CL-JEPA 的"惊喜门控""EMA 抗遗忘""潜空间 rollout 漂移"分别落地为本方案的 OnlineTrainer、第 4.3 节、第 6.1 节。本方案新增的部分是**DSH 插件侧的接口与工具对齐**（码本、ActionDecoder、GoalMapper），即把 CL-JEPA 从"独立持续学习者"接入"可调用的 Agent 框架"。

## 附录 C：关键参考文献

- LeCun (2022). *A Path Towards Autonomous Machine Intelligence*. OpenReview.
- Assran et al. (2023). *I-JEPA*. CVPR 2023, arXiv:2301.08243.
- Bardes et al. (2024). *V-JEPA*. arXiv:2404.08471.
- Assran et al. (2025). *V-JEPA 2: Self-Supervised Video Models Enable Understanding, Prediction and Planning*. arXiv:2506.09985.
- LeCun et al. (2025). *LeJEPA*（LeCun 在 Meta 最后一篇论文）.
- Huang, LeCun, Balestriero (2025). *LLM-JEPA*. arXiv:2509.14252.
- Meta AI (2025). *VL-JEPA*. arXiv:2512.10942.
- Yang et al. (2026). *Clin-JEPA*. arXiv:2605.10840.
- Fini et al. (2022). *CaSSLe*. CVPR 2022.
- Cignoni et al. (2025). *CLA*. CoLLAs 2025, arXiv:2507.10434.
