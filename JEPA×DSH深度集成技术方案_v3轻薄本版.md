# JEPA × DeepSeek Harness（DSH）深度集成技术方案（v3 · 轻薄本适配版）

> 产出方式：四位 AI 架构专家辩论委员会，基于 v2 评审结果与**硬件约束重大更正**，经 **6 轮** 严格辩论产出。
> 参与者：**架构师（Architect）**、**JEPA 专家（JEPA-Specialist）**、**DSH 专家（DSH-Specialist）**、**质疑者（Skeptic）**。
> 日期：2026-08-18 ｜ 前作：v1《…辩论委员会.md》、v2《…_v2修正版.md》。

---

## 0. 硬件约束更正与设计契约（本文档的总纲）

> 这是本轮最大的认知修正：**"20GB" 不是显存，而是模型总参数的存储上限（FP16 ≈ 100 亿参数）**。本机是轻薄本，显存极小。

| 编号 | 约束 | 数值 | 本方案达标值 |
|---|---|---|---|
| H1 | JEPA 模型总参数存储 | ≤ 20 GB（FP16 ≈ 10B 参数） | ~10B（磁盘/内存，按需加载） |
| H2 | 推理时激活参数量 | ≤ 100M | **~15M（默认）/ ~35M（最坏）** |
| H3 | 推理时显存/内存占用 | ≤ 2 GB | **< 1.1 GB（FP16 活跃缓存）/ < 600 MB（int8）** |
| H4 | 单步决策延迟 | ≤ 1.5 s | **稳态 ~30–50 ms；LLM 未命中 ~1–1.5 s（详见 §3 诚实账本）** |
| H5 | 运行环境 | 轻薄本，纯 CPU 或 CPU + 核显（**不假设独立 GPU**） | 全设计以 CPU 为准 |

**核心架构一句话**：**大规模稀疏专家池（总 ~10B，驻留磁盘/内存）+ 专家按需加载 + 极小激活（<100M）**，即"知识容量大、思考敏捷、能耗低"的类人脑形态——这正是"总参数大、激活小"的极端稀疏比工程化。

### 0.1 v2 五缺陷 → v3 修正对照表

| # | v2 缺陷 | v3 修正 | 落点 |
|---|---|---|---|
| D1 | L0 是 60M 稠密 Transformer，无字段级稀疏，"17M 激活"是假的 | 字段编码改为**字段级专家池**（4096×0.5M），字段→专家软路由，只对变化字段激活对应专家 | §2.1、辩论第2轮 |
| D2 | P0 延迟账本掩盖 LLM 成本，模板解码优势不生效 | 重写真实账本；P0 让 LLM **只生成候选动作 ID + 槽位倾向（短序列）**，参数由 JEPA 模板解码 | §3、辩论第3轮 |
| D3 | 快速权重只作用输出层，无法适应新工具后果预测 | 低秩快速权重**扩展到预测器/结果模型每一层** | §4、辩论第5轮 |
| D4 | 工具结果模型新工具无数据，rollout 会放大误差 | **分档**：热门 MLP / 冷门 LLM 先验编译 / 零数据截断降权 | §2.6、辩论第4轮 |
| D5 | 缺中层时间尺度抽象（子任务进展） | 新增**轻量时序聚合器**（进展潜 h_sub），不依赖概念涌现 | §2.5、辩论第5轮 |

---

# 第一部分：六轮辩论记录

---

## 第 1 轮：总架构与硬件适配（轻薄本 + 20GB 模型大小）

**架构师**：总架构 = **层级稀疏专家池 + 专家卸载**。总参数 10B 分成若干专家池（字段编码 2B、预测器 4B、动作 1B、工具结果模型 2B、目标映射 0.25B、共享路由 0.75B），全部驻留磁盘/内存；推理时路由只激活 ~15M。关键：**专家粒度做小（0.5M/个），小到 CPU 前向 <1ms**，这样大量专家可以留内存、CPU 按需算，不需要 GPU。

**JEPA 专家**：极端稀疏比（总/激活 ≈ 600 倍，10B/15M）不是单一 MoE 做到的，而是**三级级联稀疏**：字段级路由（选字段编码专家）+ 预测器级路由（选世界模型专家）+ 动作级路由（选解码专家）。每级 top-1~2，级联后激活量乘起来极小。JEPA 的"预测表征而非信号"在这里二次受益：因为预测对象是 128 维潜，专家可以小到 0.5M 而不失表达力。

**DSH 专家**：10B 参数不能全驻内存（轻薄本 RAM 16–32GB，OS+程序已占一部分）。方案是**分级存储**：磁盘存全量 10B，内存常驻"热专家"LRU 缓存（~512M 参数 = 1GB），活跃专家页按需加载。版本切换做**增量页切换**，不做全量 20GB 重写。

**质疑者（四问）**

> **Q1** 总/激活 600 倍稀疏比，业界 MoE 一般才 10–50 倍（DeepSeek-V3 是 671B/37B ≈ 18 倍）。你张口就是 600 倍，证据在哪？这不是"类人脑"，是"类幻想"。
> **Q2** 磁盘按需加载专家：路由命中一个冷专家，从磁盘读 0.5M 参数，HDD 随机读 10ms、SSD 0.1ms。如果每次 rollout 都要现读专家，延迟直接爆。预取怎么做？预取错了怎么办？
> **Q3** "CPU 前向 0.5M 专家 <1ms"——给算账。0.5M 参数 float32 前向 = 1M FLOPs，CPU 单核标量也就 10–50 GFLOP/s，但实际带内存带宽、激活函数、归一化，<1ms 是不是太乐观？纯 CPU 下 10 个字段编码专家到底几 ms？
> **Q4** 版本切换做"增量页"，但专家池是稠密训练出来的，一个专家变了，路由网络可能也要变，牵一发动全身，哪来"增量"？增量切换的一致性怎么保证？

**回应与修正**

- 对 Q1：**承认"600 倍"是设计目标，不是已有证据**。修正为诚实表述：**稀疏比由"三级 top-1 级联"理论可达，但 P0 阶段先用 1B 总参数（稀疏比 ~60 倍，业界可达区间）验证架构，再渐进扩容到 10B**。极端稀疏是**渐进扩容的终点**，不是起点假设。这同时回答了"过度工程"的担忧（见第 6 轮）。
- 对 Q2：**预取 + 页式管理 + 内存带宽兜底**。三层手段：① **路由预取**——路由网络先于专家计算一步算出"下一轮可能命中的专家集合"，异步预取到内存（预测式加载，类似 CPU 分支预测）；② **专家页式**——专家按 4KB 页组织，SSD 随机读 0.1ms 级，且 OS page cache 会缓存最近读过的页；③ **内存带宽兜底**——热专家常驻内存（LRU 1GB），稳态任务命中率 >90%，磁盘读是冷启动/任务切换才发生。冷启动一次性预取"本任务 schema 相关专家"，摊到整个任务可忽略。
- 对 Q3：**给诚实算账，并下调期望**。0.5M 参数前向 ≈ 1M FLOPs；但考虑内存带宽（读取 0.5M×4B=2MB 权重，DDR4 ~20GB/s → 0.1ms 只是读权重）+ 逐层访存，**单专家前向取保守 0.5–2ms**，10 个变化字段 ≈ 5–20ms。修正 §3 账本用这个保守值。结论不变（仍远小于 LLM 的秒级）。
- 对 Q4：**承认"增量切换"的边界**。改为：**专家权重增量切换**（只换变动的专家页）+ **路由网络全量切换但体积小**（路由 ~0.75B，但实际生效的路由表是稀疏的哈希表，切换是换指针 + 重建索引，毫秒级）。一致性靠**版本号 + 快照读**（沿用 v2 的 MVCC）：每个决策步开始时固定版本，本步内路由+专家一致，下步自然切新版本。

**本轮结论**
1. 架构 = 层级稀疏专家池（总 10B）+ 专家卸载 + 三级级联稀疏。
2. 极端稀疏比是渐进扩容的**终点**（P0 先 1B 验证），非起点假设。
3. 专家按需加载靠：路由预取 + 4KB 页式 + 热专家 LRU 常驻内存。
4. 单专家 CPU 前向保守 0.5–2ms（非 <1ms）；版本切换 = 专家增量 + 路由换指针 + MVCC 快照读。

---

## 第 2 轮：真正稀疏的状态编码器（修正缺陷 D1）

**JEPA 专家**：v2 的 L0 是 60M 稠密 Transformer，**"只对变化字段跑前向"在稠密 Transformer 上物理不成立**——D1 的批评完全正确。修正为**字段级专家池**：

- **字段编码专家池**：E=4096 个字段编码专家，每个 0.5M（2 层 d=256 的轻量块）。
- **字段→专家路由**：两级——先**语义 hash**（字段名/字段文本的快速 hash 到 64 桶，粗分桶），再**桶内软路由**（小 MLP 选桶内 top-1 专家）。
- **增量激活**：未变化字段直接取字段原型缓存向量，**连路由都不跑**；只有变化字段走"路由→专家前向"。

**架构师**：字段间依赖不在字段编码层解决，而在 L1 聚合层（跨字段 attention，~5M，共享）解决。这是标准的两阶段编码：字段编码是"局部感知"，聚合层是"全局整合"。这样字段编码专家可以真正独立、可稀疏。

**DSH 专家**：字段原型缓存沿用 v2（每工具 schema 分片），热槽 1024，LRU+EMA。字段是否变化用**语义 hash + 余弦距离**判定，值变但语义近不重编。

**质疑者（四问）**

> **Q1** 4096 个字段编码专家，每个只见过自己的字段？字段是**无限**的（不同工具不同 schema，字段名千变万化），专家池有限，字段→专家怎么映射？硬 hash 会碰撞（两个不同字段挤进同一专家），软路由要训练——**训练信号是什么**？字段没有标签。
> **Q2** 你说"未变字段连路由都不跑"，但**怎么知道哪个字段变了**？还是要先把新 JSON 和旧 JSON 做 diff，diff 本身要解析 JSON、逐字段比较，这个成本不省。
> **Q3** 字段编码专家 0.5M（2 层 d=256）能编码什么？一个 JSON 字段文本可能几百 token（一个长字符串字段），0.5M 的编码器连上下文都放不下，字段语义编码质量堪忧。
> **Q4** 给数字：总参数量、默认激活（50 字段变化 10）、最坏激活（50 字段全变），以及这三档的延迟。

**回应与修正**

- 对 Q1：**训练信号 = LLM-JEPA 式的潜预测，不需要字段标签**。字段编码专家池是**从一个小基础编码器通过"专家分裂"长出来的**（progressive MoE：共享起步 → 按路由负载分裂专家 → 各自特化）。软路由的训练信号就是 JEPA 的预测误差回传：让"预测下一状态更准的字段路由"被强化。**字段→专家不是静态映射，是学出来的软聚类**。硬 hash 只做粗分桶（降低软路由搜索空间），碰撞由桶内软路由解。
- 对 Q2：**承认 diff 成本存在，但极小且可复用**。JSON 解析 + 逐字段 hash 是 O(字段数) 的字符串操作，50 字段 diff <1ms（相比 LLM 秒级可忽略）。且 diff 结果本身喂给"字段变化检测"，不重复计算。**真正的收益不是省 diff，而是省"重编码"**：变化字段 10 个只编码 10 个，而非 50 个全重跑稠密编码器。
- 对 Q3：**长字段做分块，块内独立编码 + 块间池化**。字段文本 >N token 时切成块，每块路由到一个字段专家编码，块向量池化得字段向量。0.5M 专家编码 128 token 的块是够的（d=256, 2 层）。**代价**：长字段激活多个专家，激活量随字段长度增长，但字段长度远小于总字段数，仍可控。
- 对 Q4：**给数字**。

  | 档位 | 激活量 | 延迟（CPU 保守） |
  |---|---|---|
  | 默认（50 字段变化 10） | 10×0.5M + 聚合 5M + 路由 1M ≈ **11M** | ~10–20 ms |
  | 最坏（50 字段全变，含长字段分块） | ~50×0.5M + 聚合 + 路由 ≈ **31M** | ~30–60 ms |
  | 总参数量 | 字段池 2B + 聚合 5M | **~2B** |

  两档均 <100M（H2），延迟均在 LLM 秒级面前可忽略。

**本轮结论**
1. 状态编码器 = 字段级专家池（4096×0.5M，专家分裂生长）+ 两级路由（hash 粗分桶 + 软路由）+ 跨字段 attention 聚合层。
2. 真正稀疏：变化字段才路由+编码，未变字段取缓存，激活随变化字段数线性增长。
3. 训练信号 = JEPA 潜预测误差回传（无字段标签），软路由学出字段软聚类。
4. 默认激活 ~11M、最坏 ~31M，总 ~2B；延迟 10–60ms。

---

## 第 3 轮：P0 延迟账本与 LLM 成本真实化（修正缺陷 D2）

**架构师**：v2 说"主循环 20–60ms"是**虚假账本**——P0 每步都要 LLM 先生成候选。真实账本必须把 LLM 放进去。**关键修正：P0 让 LLM 只生成"候选动作 ID + 槽位倾向"（~8–15 token 短序列），完整参数由 JEPA 模板/分类器解码**。这样模板解码优势在 P0 就生效，且 LLM 生成量从"50–100 token 的完整 JSON"降到"8–15 token 的短候选"。

**DSH 专家**：轻薄本上 LLM 是延迟主导项，必须量化。用 llama.cpp 类框架，**0.5B 参数 int4 量化，CPU 8 核，生成速度约 5–15 token/s**（内存带宽受限）。15 token ≈ 1–3s，50–100 token ≈ 4–10s。**结论：纯 LLM 完整生成参数在轻薄本上根本不可用**。

**JEPA 专家**：P0 的净价值因此被重新定义——**不是"排序提升动作质量"，而是"让 LLM 少干活，从而把 Agent 从不可用变成可用"**。JEPA 排序 20–40ms 相对 LLM 是零头，但它换掉了 LLM 生成 50+ token 的负担。

**质疑者（四问）**

> **Q1** LLM 只生成"候选动作 ID + 槽位倾向"，那**排序时 JEPA 用什么参数去 rollout**？参数还没生成。排序的对象是"半成品动作"吗？这逻辑上自洽吗？
> **Q2** LLM 短候选（15 token）本身够不够准确？如果 LLM 为了省 token 而候选质量下降，JEPA 排序再好也是在垃圾里挑，P0 的整体成功率会不会反而比"LLM 完整生成"更低？
> **Q3** 轻薄本上 0.5B int4 生成 15 token 要 1–3s，已经逼近甚至超过 1.5s 预算（H4）。**单步延迟约束到底能不能满足**？还是 H4 其实只能在"动作缓存命中"时满足？
> **Q4** 给真实的对比账本：纯 LLM（完整参数）vs LLM 短候选 + JEPA 排序 + 模板解码，各自的延迟、显存、成功率预期。

**回应与修正**

- 对 Q1：**排序对象是"完整动作"，参数由 JEPA 先解码再排序**。流程：LLM 生成 `{候选动作ID, 槽位倾向}` → **JEPA 的 ParamDecoder 立即用状态上下文 + 槽位倾向解码出完整参数**（模板+分类器，<5ms）→ 得到完整动作候选 → JEPA 用完整参数做 rollout 排序。所以 LLM 输出的是"动作意图"，JEPA 把意图落成完整动作再排序。逻辑自洽，且解码成本 <5ms。
- 对 Q2：**承认这是 P0 的头号风险，且它排在 JEPA 之前**。诚实结论：**P0 的成败首先取决于"LLM 短候选的准确率"，其次才是 JEPA 排序**。缓解：① 动作原型缓存——高频动作（"打开某文件""查询某表"）直接命中缓存，**跳过 LLM**（命中时延迟降到 JEPA 排序 30–50ms）；② 缓存未命中时 LLM 短候选 + 置信门控——候选置信低就回退"LLM 完整生成"（慢但准）。**用缓存 + 两级 LLM 兜底，把"短候选不准"的风险圈在少数冷门动作里**。
- 对 Q3：**诚实承认 H4 的分层满足**：动作缓存命中（稳态，占比预期 >70%）→ 单步 30–50ms，远达标；LLM 短候选未命中 → 1–2s，**逼近或略超 1.5s**。修正 H4 的表述为：**"稳态（缓存命中）单步 ≤1.5s 且余量充足；LLM 未命中单步目标 ≤1.5s，需靠更小模型/更多缓存逼近"**。同时用**异步流水线**（LLM 生成与上一步的 JEPA 后处理并行）压缩感知延迟。不粉饰：这是轻薄本上 LLM 的物理极限，JEPA 的意义正是**让大多数步不走 LLM**。
- 对 Q4：**对比账本（CPU 8 核，0.5B int4 LLM）**：

  | 方案 | LLM 生成 | 参数解码 | JEPA 排序 | 总延迟 | 显存 |
  |---|---|---|---|---|---|
  | 纯 LLM 完整生成 | 50–100 token ≈ 4–10s | LLM 内 | — | **4–10s** | LLM ~0.4GB |
  | LLM 短候选 + JEPA（未命中） | 15 token ≈ 1–3s | <5ms | 20–40ms | **~1–3s** | +JEPA 缓存 |
  | 动作缓存命中（稳态） | **0** | <5ms | 20–40ms | **~30–50ms** | 同上 |

  净收益：稳态快 2 个数量级；未命中时也快 3–5 倍。**代价**：缓存未命中时多一层 JEPA 依赖，若短候选不准需回退完整生成（退化到纯 LLM）。

**本轮结论**
1. P0 真实账本 = LLM 短候选（8–15 token）+ JEPA 模板解码 + JEPA 排序；排序对象是"JEPA 先解码出的完整动作"。
2. 轻薄本 LLM 是延迟主导（0.5B int4 ≈ 5–15 token/s）；纯 LLM 完整生成 4–10s 不可用。
3. P0 净价值 = 让 LLM 少干活（短候选 + 缓存），把 Agent 从不可用变可用；排序提升是次要收益。
4. H4 分层：缓存命中 30–50ms；未命中 1–3s（逼近上限，靠更小模型/更多缓存逼近）。

---

## 第 4 轮：工具结果模型分档与 rollout 可靠性（修正缺陷 D4）

**JEPA 专家**：工具结果模型分三档（对应数据量）：

- **热门工具（轨迹 ≥1000 条）**：轻量 MLP 学 `(状态潜, 参数潜) → 状态潜增量`，每工具一个专家（~0.5M），离线训练，在线快速权重微调。
- **冷门工具（10–1000 条）**：**LLM 离线生成先验转移规则**，编译成"模板式状态潜增量函数"（对状态潜的特定维度做条件赋值，如"调用 HTTP 后，`response` 维 ← 请求结果"），再用少量数据校准。这是**编译出来的先验**，不是学出来的模型。
- **零数据新工具**：rollout 中**截断该分支 + 标记最高不确定度 + 降评分权重**（cost 打高惩罚），并触发"试探动作"（专门执行一次以采集数据）。

**架构师**：rollout 里加一个**可信度标量**随步传播：`conf_t = conf_{t-1} × (1 − 归一化预测方差)`，低于阈值就截断该分支。这样工具结果模型的误差不会无界累积。

**DSH 专家**：先验规则由 LLM 在**工具注册时离线生成一次**（读 schema + docstring），存成结构化规则，编译成增量函数，不进推理热路径。

**质疑者（四问）**

> **Q1** 冷门工具的"先验转移规则"——"调用 HTTP 后 response 维 ← 请求结果"，这个"←"怎么在 128 维潜空间里执行？潜空间维度是学出来的、无语义的，你**怎么知道 response 对应哪个维度**？"语义规则 → 潜空间增量"的映射根本不存在，这是幻想。
> **Q2** 热门工具结果模型离线训练，但**在线用快速权重微调**——结果模型用自己的预测结果再训练自己，会不会自举污染、误差越训越大？
> **Q3** 零数据新工具"截断分支"——如果一个任务的所有候选都涉及新工具，所有分支都被截断，**排序退化成随机**，怎么办？谁来破局？
> **Q4** 可信度阈值怎么设？设高了误截断好分支（探索不足），设低了误差累积（信任过度）。阈值在线上漂移吗？

**回应与修正**

- 对 Q1：**承认"语义维度赋值"在学出的潜空间上不成立，修正为"规则作用于可解释的中间表示"**。引入一个**可解释的槽位层**：状态潜不是唯一的表示，我们额外维护一个**离散状态槽位字典**（`{key: value}` 的稀疏表示，如 `{"response_status": "200", "has_file": true}`），它是**人类可读、schema 对齐**的。先验规则编译成**槽位层的转移函数**（"HTTP 成功后 `response_status` 槽 ← 200"），这是可计算、可解释的；槽位层再通过一个**槽位↔潜 投影**（可训练，~1M）映射回潜空间供 rollout 用。**即：规则作用于可解释槽位层，潜空间只做投影**，不幻想"语义维度赋值"。
- 对 Q2：**结果模型在线只用快速权重，且用"真实回报"做监督，不自举**。快速权重更新用的是**真实工具返回后编码的 s_{t+1}**（ground-truth 反馈），不是结果模型自己的预测。结果模型的自回归（用自己输出当下一输入）只发生在 rollout 内部、且被可信度截断兜住。所以"训练"和"rollout 自举"是分开的两条路径，不污染。
- 对 Q3：**全截断时回退 LLM 直接决策（不排序）**，同时强制执行一次"试探动作"采集新工具数据。破局逻辑：**新工具的第一次使用必须真实发生，代价由 LLM 兜底承担，收益是下次有数据**。这是探索的显式代价，不是系统缺陷。
- 对 Q4：**阈值用"校准集 + 在线分位数"**。初始值在离线校准集上标定（取"截断后 P@1 不降"的最大阈值）；线上用历史 conf 的分位数自适应（同 v2 的 τ 自适应思路）。设保守（宁可截断，不误信），因为截断的成本（回退 LLM）远低于误信的成本（执行错误动作）。

**本轮结论**
1. 工具结果模型三档：热门 MLP / 冷门 LLM 先验规则编译 / 零数据截断+试探。
2. 先验规则作用于**可解释槽位层**（离散 key-value），经槽位↔潜投影回潜空间，不幻想语义维度赋值。
3. 结果模型在线用真实反馈 + 快速权重，不参与 rollout 自举。
4. 可信度标量随 rollout 传播 + 阈值在线分位数自适应；全截断回退 LLM + 试探动作破局。

---

## 第 5 轮：跨时间抽象与快速适应（修正缺陷 D3、D5）

**JEPA 专家**：两处修正：

1. **跨时间抽象（D5）**：新增**时序聚合器**——一个轻量 GRU（~1M），把子任务内最近 N 步的 `(状态潜, 动作潜)` 压缩成**进展潜 `h_sub∈ℝ^64`**。这是"秒~分钟级"的中层时间表征，不聚类成符号，就是连续时序压缩。用途：① 子任务完成检测（h_sub 与 g_sub 余弦距离）；② 异常漂移检测（h_sub 变化率异常）；③ 为 MPC 提供历史上下文（h_sub 拼接到状态潜）。
2. **快速权重扩展到深层（D3）**：低秩快速权重（rank=8）挂在**预测器/结果模型的每一层**，用增量岭回归更新。这样新工具的行为后果可以在**秒~分级**用快速权重近似，而不是等分钟级 LoRA。

**DSH 专家**：子任务完成判据由 LLM 生成子目标时**同时输出**（可检验的状态条件），自动标注轨迹里的子任务边界 → 训练完成检测器。不靠人工。

**质疑者（四问）**

> **Q1** GRU 压缩 N 步，N 取多少？子任务长度从 2 步到 200 步不等，固定 N 怎么适应？GRU 在线用不 BPTT，长依赖靠什么维持？
> **Q2** 快速权重"挂在每一层"——低秩 rank=8，预测器 2 层，每层快速权重多少参数？增量岭回归在中间层维度（256）下，A 矩阵 256×256 求逆，比输出层贵多少？还是 μs 级吗？
> **Q3** 快速权重和"分档结果模型"是什么关系？热门工具已经有 MLP 结果模型，又叠快速权重，是不是重复？
> **Q4** 轻薄本上 head 梯度更新的真实延迟：反传要保存激活、要算梯度、要更新 30M 参数，CPU 上到底多少 ms？异步更新会不会追不上任务节奏？

**回应与修正**

- 对 Q1：**GRU 天然处理变长（隐藏态跨步传递），无需固定 N**。长依赖靠两个机制：① GRU 隐藏态本身就是有损长期记忆；② 超出 GRU 窗口的关键节点（子目标切换点）用**进展潜快照**留存（每次子目标切换存一个 h_sub 快照到原型库）。"秒~分钟级"的抽象不需要精确的长程记忆，GRU + 快照足够。
- 对 Q2：**给数字**。预测器 2 层（192→256→128），每层低秩快速权重 = (d_in+d_out)×rank = (192+256+256+128)×8 ≈ 6.6K 参数，两层 ~13K，极小。**中间层增量岭回归**：A 矩阵 256×256，求逆 O(256³)≈16M FLOPs，CPU 上 <5ms（相对输出层 64×64 的 μs 级变慢，但仍远小于 LLM）。**修正**：深层快速权重的更新频率降为"每 N=8 步一次"（而非每样本），把 5ms 摊薄。
- 对 Q3：**不重复，是两层互补**。MLP 结果模型 = 该工具的**静态先验**（离线学，覆盖常见情况）；快速权重 = 该工具**最近的行为偏移**（在线学，覆盖"这个工具最近被这样用"的局部变化）。类似"长期知识 + 短期工作记忆"。新工具没有 MLP 先验时，快速权重是**唯一**的在线近似手段（D3 的修正点）。
- 对 Q4：**诚实延迟**。head（~30M）一次反向+更新，CPU 8 核约 **100–300ms**（取决于 batch 和框架优化）。异步后台执行，**不阻塞主循环**。会不会追不上任务节奏？——**会，但没关系**：主循环决策用的是"上一步已更新到的 head 快照"，梯度更新是滞后的、异步的，这是在线学习的常态（类似人边做边总结，不要求即时）。若需要更激进，把更新 head 缩小到 actor+critic（~15M），延迟减半。

**本轮结论**
1. 跨时间抽象 = 轻量 GRU 进展潜 h_sub（连续时序压缩，不依赖概念涌现）+ 子目标快照。
2. 子任务完成判据由 LLM 随子目标输出，自动标注训练数据。
3. 快速权重 = 低秩（rank 8）挂预测器/结果模型每层，增量岭回归；中间层 256×256 求逆 <5ms，降频到每 8 步。
4. head 梯度 CPU 100–300ms，异步不阻塞；快速权重与结果模型是"短期偏移 vs 静态先验"互补。

---

## 第 6 轮：整体可行性重评估与 MVP 路线图修正

**架构师**：修正后渐进路线依然成立，但**P0 的门槛前置了**：P0 的第一验证目标不是 JEPA 排序，而是"LLM 短候选 + 动作缓存"能否让 Agent 在轻薄本上可用。JEPA 的排序收益是第二步验证。

**JEPA 专家**：总参数 10B 是**扩容终点**，MVP 从 1B 起步（专家分裂渐进扩容），避免"为了 10B 而 10B"的过度工程。

**DSH 专家**：每阶段明确部署形态（磁盘/内存/活跃）与量化指标、止损线。

**质疑者（四问）**

> **Q1** 修正后 P0 的真实成败关键变成"LLM 短候选准确率"，那 JEPA 在 P0 到底还剩下多少戏份？如果短候选本身就准，JEPA 排序是锦上添花；如果不准，JEPA 也救不了。JEPA 会不会在 P0 被边缘化？
> **Q2** 各阶段部署形态：1B 起步时，磁盘/内存/活跃各存多少？扩容到 10B 时呢？给数字。
> **Q3** 止损线：如果 P0 证明"LLM 短候选 + 缓存"可行但 JEPA 排序无收益，降级"世界模型哨兵"在轻薄本上怎么落地？哨兵跑一次前向多少延迟？
> **Q4** 为了极端稀疏比引入专家分裂、专家卸载、页式管理、MVCC、分级存储……这套复杂度是不是**为了一个 10B 的虚荣数字**而过度工程？有没有更简单的路径达到"轻薄本可用 Agent"这个真实目标？

**回应与修正**

- 对 Q1：**承认 JEPA 在 P0 的角色是"降本增效的放大器"，不是"决策核心"**。具体戏份：① 参数解码（模板+分类器）是 JEPA 独有、LLM 替代不了——**这才是 P0 JEPA 的第一价值**（把 LLM 从参数生成里解放出来）；② 排序是第二价值。所以即使排序无收益，JEPA 的**参数解码**已经在 P0 立住了。这修正了 v2 把 JEPA 价值压在"排序"上的偏差。
- 对 Q2：**给数字**。

  | 阶段 | 总参数 | 磁盘 | 内存（热专家 LRU） | 活跃（激活） |
  |---|---|---|---|---|
  | P0（1B） | 1B | 2GB | 512M | ~15M |
  | P4（10B） | 10B | 20GB | 1GB | ~15–35M |

  扩容到 10B 时，磁盘 20GB（SSD 可承受），内存 LRU 只增到 1GB（热专家不随总量线性涨），激活不变——**这正是"总参数大、激活小"的红利：扩容只涨磁盘，不涨延迟和内存**。
- 对 Q3：**哨兵 = 只跑状态编码 + 预测器（不规划、不解码）**。单步前向 = 状态编码（10–20ms）+ 预测器（<5ms）≈ **15–30ms**。哨兵形态：实时输出"当前动作的预测误差 E"，E 超阈即预警（拦截疑似幻觉动作/成本失控）。**在轻薄本上完全可行，且独立有用**（LLM Agent 的幻觉动作是真实痛点）。
- 对 Q4：**诚实回应，砍掉过度工程**。真实目标 = "轻薄本可用的、低成本的、能自适应的 Agent"。达成它**不必然需要 10B**。修正：**MVP（P0–P2）只用 1B 总参数、dense 字段编码器起步**（不引入专家卸载/页式/分级存储，先用最简单的 dense 小模型把"LLM 短候选 + JEPA 参数解码 + 排序"这套跑通）；**只有 P3 验证了"扩容能涨知识容量且不涨延迟"的假设后，才逐步引入专家分裂→专家卸载→10B**。即：**稀疏化和扩容是 P3 之后的优化项，不是 MVP 的前置复杂度**。这是本轮对"过度工程"质疑的正面让步。

**本轮结论**
1. JEPA 在 P0 的第一价值 = **参数解码**（LLM 替代不了），排序是第二价值；不再把宝全压在排序上。
2. 扩容只涨磁盘（1B→10B 磁盘 2GB→20GB），内存 LRU 和激活不变——稀疏化红利。
3. 哨兵 = 只跑编码+预测器，15–30ms/步，轻薄本可行且独立有用。
4. **MVP 用 dense 1B 起步，稀疏化/专家卸载/10B 是 P3 后的渐进优化，砍掉前置过度工程**。

---

# 第二部分：技术方案文档（v3）

---

## 1. 总体架构（轻薄本适配版）

### 1.1 插件模块与部署位置

| 模块 | 职责 | 部署位置 | 参数量 |
|---|---|---|---|
| `StateEncoder` | 字段级专家池 + 聚合层 → 状态潜 `s∈ℝ^128` | 专家池磁盘/内存，聚合层常驻 | 池 2B，聚合 5M |
| `WorldModel`（预测器专家池） | `Δŝ = g_φ(s,a)` 潜残差预测 | 专家池磁盘/内存 | 池 4B |
| `ActionCodebook` | 工具码本（簇+工具层） | 常驻内存 | ~几 M |
| `ParamDecoder` | 模板 + 分类器参数解码 | 常驻内存 | ~几 M |
| `GoalMapper` | 目标 → `g∈ℝ^128` + OOD 置信 | 专家池 + 常驻投影 | 池 0.25B |
| `Planner` | 低层 MPC + UCB + 失败检测 | 常驻内存 | 逻辑模块 |
| `Critic` | 状态/轨迹代价 | 常驻内存 | ~几 M |
| `TemporalAggregator` | GRU 进展潜 `h_sub∈ℝ^64` | 常驻内存 | ~1M |
| `ToolResultModel` | 分档结果模型（热门/冷门/零数据） | 专家池磁盘/内存 | 池 2B |
| `OnlineTrainer` | 秒级适应 + 异步梯度 | 常驻内存 | — |
| `OfflineConsolidator` | 离线巩固（远程/异步） | 远程或本机空闲时 | — |
| `LLM` 插件 | 短候选生成 + 子目标分解 + 自由文本参数 + 兜底 | 常驻内存（int4） | 0.5B int4 ≈ 0.4GB |

### 1.2 完整任务周期数据流（纯文本/JSON）

```
用户目标(文本) → GoalMapper → g∈ℝ^128, conf
   │ conf<τ → 澄清(≤1轮) → 回退LLM
   ▼
JSON观察 → 字段diff → 变化字段 → 字段专家池(仅变化字段) → 聚合 → s∈ℝ^128
   │ (未变字段取原型缓存, 不路由不编码)
   ▼
┌──────── P0 决策循环 ────────┐
│ 动作缓存命中? → 是: 直接取缓存动作(跳过LLM)      │
│             → 否: LLM生成短候选{动作ID+槽位倾向} │
│ ParamDecoder: 模板+分类器 → 完整动作候选         │
│ Planner: 对每个候选 rollout(预测器+工具结果模型) │
│          + 可信度截断 + UCB排序 → 选最优          │
└────────────────────────────┘
   │
   ▼ 执行插件 → 新JSON观察
   │
   ├─ TemporalAggregator: 更新 h_sub(进展潜)
   ├─ 预测误差 E = ‖ŝ_{t+1} − s_{t+1}‖
   │    ├─ 秒级适应(校准/原型/快速权重, 异步)
   │    ├─ E持续高 → 低层重编码/异步head梯度/LoRA
   │    └─ E单调升 → 失败检测 → 重规划/交还LLM
   └─ 轨迹日志 + latent sidecar
```

### 1.3 如何实现"总 10B / 激活 <100M / 显存 <2GB"

- **总 10B**：六大专家池（字段 2B + 预测 4B + 动作 1B + 结果模型 2B + 目标 0.25B + 路由 0.75B）驻留磁盘，内存仅热专家 LRU（~1GB）。
- **激活 <100M**：三级级联稀疏——字段级 top-1（10 变化字段激活 10 个专家）、预测器 top-2、动作/目标/结果模型各 top-1，级联后默认 ~15M、最坏 ~35M。
- **显存 <2GB**：活跃专家 LRU 缓存（int8 ~600MB）+ 共享层常驻（~14MB）+ 激活缓冲（<50MB）+ LLM int4（~0.4GB），合计 **<1.1GB**。

---

## 2. 核心模块设计

### 2.1 状态编码器（真正稀疏，修正 D1）

| 组件 | 结构 | 参数量 |
|---|---|---|
| 字段编码专家池 | 4096 个专家 × 0.5M（2 层 d=256），字段文本分块后逐块编码 | 2.0B |
| 两级路由 | 语义 hash（64 桶粗分）+ 桶内软路由 MLP（top-1） | 1M（共享） |
| 字段聚合层 | 跨字段 attention + 门控（输出 mean+var） | 5M |
| 状态池化 | 2 层 MLP → `s∈ℝ^128` | 2M |

- **输入**：JSON 序列化；**输出**：`(s∈ℝ^128, var∈ℝ^128)`。
- **稀疏激活**：字段 diff → 变化字段才路由+编码；未变字段取原型缓存（**连路由都不跑**）。
- **激活量**：默认 ~11M，最坏 ~31M（均 <100M）。
- **训练**：字段专家池由小基础编码器经**专家分裂**（progressive MoE）生长，信号 = LLM-JEPA 式潜预测误差回传（无字段标签）；聚合层 + 池化层常规训练。
- **字段间依赖**：由跨字段 attention 聚合层补偿（两阶段编码）。

### 2.2 预测器（MoE 专家池，适配轻薄本）

- **结构**：输入 `(s∈128, a∈64, h_sub∈64)`（拼接 256）→ 路由（top-2）→ 2 个专家（256→256→128）→ 输出 `Δŝ∈128`。
- **专家池**：8192 个专家 × 0.5M = 4B；激活 top-2 = 1M。
- **路由**：auxiliary load-balancing loss + 噪声注入防坍缩；轨迹内路由一致性正则。
- **训练**：`L = ‖Δŝ + s_t − sg(s_{t+1})‖²`，EMA target（stop-grad）；集成 4 个预测器（输出均值+方差）。
- **漂移控制**：残差模式 + EMA target + 集成方差截断（rollout 中方差超阈截断）。
- **升级路径**：专家数 8192→32768（P3 后），输入加 4 步历史。

### 2.3 动作码本与参数解码（模板优先，P0 即生效）

- **码本**：簇层 32 × 工具层 K≤512，工具码 = LLM 编码 `(name+docstring+schema)` 投影 ℝ^64。
- **参数解码**：`enum/boolean` → 槽位原型最近邻；`number` → 回归；`structured` → 递归模板；`free-text` → LLM（仅此类）。
- **分类器**：共享槽位注意力 + 槽位原型库（零训练扩展）。
- **P0 生效机制**：LLM 只输出 `{动作ID + 槽位倾向}`，ParamDecoder 据此 + 状态上下文解码完整参数——**模板优势从 P0 第一天就生效，不再被 LLM 完整生成架空**。

### 2.4 目标映射器（原型检索 + OOD，控存储）

- **结构**：LLM 编码 → 投影（2 层 MLP）→ 目标原型 kNN 插值校正 → `g∈ℝ^128` + `conf`。
- **OOD**：`conf = −min_dist(g, 目标原型库)`；阈值 τ(t) = 历史 conf 10% 分位（EMA 自适应）。
- **存储**：目标原型库 1024×128 = 0.13M 参数，忽略不计。
- **训练**：InfoNCE + 困难负例挖掘；冷启动 LLM 影子 bootstrap + 文本嵌入临时原型。

### 2.5 规划器（低层 MPC + 子目标 + 跨时间抽象，修正 D5）

- **低层 MPC**：K=64 候选 × H=16 步；rollout 只跑预测器（1M 激活），不重编码；滚动重规划。
- **历史上下文**：进展潜 `h_sub`（GRU，1M）拼接到状态潜，MPC 不再只看当前状态。
- **长时程**：LLM 显式子目标（含完成判据）→ 编码 g_sub → 低层 MPC 逐个执行；**完成检测**用 `cos(h_sub, g_sub)`。
- **UCB**：`score = 预测代价 − κ·不确定度`；不确定度 = 集成方差（Plan B：显式 σ 头）。
- **失败检测**：E 单调升 / cost 升 / 完成判据不满足 → 重规划或交还 LLM。

### 2.6 工具结果模型（分档，修正 D4）

| 档位 | 条件 | 模型 | 在线更新 |
|---|---|---|---|
| 热门 | 轨迹 ≥1000 | MLP `(s, a_param) → Δs`（0.5M/工具） | 快速权重 |
| 冷门 | 10–1000 | LLM 先验规则编译到**可解释槽位层** + 槽位↔潜投影 | 少量数据校准 |
| 零数据 | 0 | rollout 截断 + 最高不确定度 + 降评分 + 试探动作 | 首次真实执行后转冷门 |

- **可解释槽位层**：离散 `{key: value}` 稀疏表示，规则作用于槽位层（可计算、可解释），经投影回潜空间——避免"语义维度赋值"的幻想。

---

## 3. P0 延迟与成本账本（真实化）

### 3.1 分步账本（轻薄本 CPU 8 核，0.5B int4 LLM ≈ 5–15 token/s）

| 步骤 | 操作 | 延迟 |
|---|---|---|
| 1. 字段 diff | JSON 解析 + 逐字段语义 hash | <1 ms |
| 2. 状态编码 | 变化字段 × 字段专家（0.5–2ms/个） | 10–60 ms |
| 3. 动作缓存查表 | 命中则跳过 LLM | <1 ms |
| 4a. LLM 短候选（未命中） | 生成 8–15 token | **1–3 s（主导项）** |
| 4b. 动作缓存（命中） | 直接取缓存动作 | 0 |
| 5. 参数解码 | 模板 + 分类器 | <5 ms |
| 6. JEPA 排序 | K=8 × H=8 × 预测器 1M | 20–40 ms |
| **合计（缓存命中）** | | **~30–50 ms** |
| **合计（未命中）** | | **~1–3 s** |

### 3.2 与纯 LLM 方案对比

| 方案 | 延迟 | 显存 | 说明 |
|---|---|---|---|
| 纯 LLM 完整生成 | 4–10 s | ~0.4GB | 轻薄本上**不可用** |
| LLM 短候选 + JEPA（未命中） | 1–3 s | +JEPA 缓存 | 快 3–5 倍 |
| JEPA 缓存命中（稳态） | 30–50 ms | 同上 | 快 2 个数量级 |

### 3.3 净收益/代价

- **净收益**：稳态（缓存命中）延迟降低 2 个数量级；未命中降低 3–5 倍；参数解码从 LLM 解放（JEPA 独有价值）。
- **代价**：新增 JEPA 依赖；缓存未命中且短候选不准时回退完整生成（退化到纯 LLM）。
- **诚实声明**：单步 ≤1.5s 在"缓存命中"时富余满足；"未命中"时逼近上限，靠动作缓存覆盖率（目标 >70%）+ 更小 LLM 逼近。

---

## 4. 实时训练与适应性机制

### 4.1 三级适应

| 级别 | 组件 | 算法 | 延迟（CPU） |
|---|---|---|---|
| 秒级（非梯度） | LayerNorm 统计 | EMA 重估（校准） | <1 ms |
| 秒级（非梯度） | 字段/目标/槽位原型库 | 在线 EMA + k-means 一步 | <1 ms |
| 秒~分级（梯度，head-only） | actor/critic/分类器 head | 异步小批量梯度 | 100–300 ms/步（后台） |
| 秒~分级（快速权重） | 预测器/结果模型**每层**低秩快速权重 | 增量岭回归（rank 8，中间层 256×256 求逆 <5ms，每 8 步一次） | <5 ms |
| 分钟级（LoRA） | 主干专家 | 触发式 LoRA 残差 | 1–3 s/batch（后台） |

### 4.2 快速权重扩展方案（修正 D3）

- **位置**：预测器、结果模型的每一层（不只输出层）。
- **形式**：低秩 `W_fast = B·A`（rank 8），总快速权重 ~13K（预测器）+ 结果模型若干。
- **更新**：增量岭回归（维护 A=Σxxᵀ+λI、B=Σxyᵀ，闭式求逆）；深层更新降频到每 8 步。
- **作用**：新工具行为后果的秒~分级近似，弥补"结果模型无数据"的空窗。

### 4.3 遗忘控制（大总参数下的显存考量）

1. 专家池**默认冻结**（遗忘为零），仅激活的 head/快速权重更新。
2. 激活专家的 EMA 慢副本蒸馏（钉住旧表示）。
3. 原型重放（旧领域字段原型）。
4. 快速权重/head 与专家主干隔离（不写主干）。
5. LoRA 隔离 + 可回滚。
6. **显存注意**：EMA 慢副本只对**激活的专家**维护（不是全 10B），显存开销 = 激活专家数 × 2，可忽略。

---

## 5. 离线训练与巩固流程（轻薄本异步方案）

轻薄本不承担离线巩固的重活，方案：

1. **本机采集**：轨迹日志（含 latent sidecar）持续落盘，只采集不训练。
2. **远程/异步巩固**：本机空闲（夜间/合盖充电）或推送到远程服务器，执行离线巩固四件事——原型蒸馏、码本更新、目标映射器重训、预测器精修 + 专家分裂扩容。
3. **产物回传**：巩固产出"新版本专家页 + 路由表"，增量回传本机，安全边界原子切换（MVCC）。
4. **版本协调**：离线为主、在线为补；离线合并在线攒下的 head 梯度统一基线。

---

## 6. 渐进式实施路线图（修正版）

| 阶段 | 总参数 | 交付 | 部署形态 | 验证指标 / 止损线 |
|---|---|---|---|---|
| **P0 预测辅助** | 1B（dense 起步） | 状态编码 + 动作缓存 + LLM 短候选 + JEPA 参数解码 + 排序 | 内存 512M 热专家，无专家卸载 | ① 短候选+缓存使稳态延迟 ≤50ms；② 参数解码正确率 ≥95%；③ 排序 P@1 提升；止损：短候选准确率 <80% → 加大缓存/回退完整生成 |
| **P1 世界模型** | 1B | 预测器精度 + rollout 稳定性（A1 门槛） | 同上 | E 低于随机基线；rollout 16 步漂移可控；止损：漂移失控 → 缩短 H |
| **P2 半自主** | 1B | JEPA 采样 + 置信门控 + 交还 LLM | 同上 | P@1 ≥+5%、top-1 ≥90%×基线；止损：不达标 → 维持 P0 |
| **P3 自主 + 扩容** | 1B→10B | 专家分裂 → 专家卸载 → 分级存储 | 磁盘 20GB，内存 LRU 1GB | 扩容不涨延迟（激活不变）；止损：扩容无收益 → 停在 1B |
| **P4 巩固闭环** | 10B | 秒级适应全量 + 远程巩固 + LoRA | 同上 | 新工具秒~分级适应成功率；遗忘率 BWT 可控 |

**核心修正**：稀疏化、专家卸载、10B 扩容是 **P3 之后的优化项**，MVP（P0–P2）用 dense 1B 先把"LLM 短候选 + JEPA 参数解码 + 排序"跑通，砍掉前置过度工程。

---

## 7. 可行性分析与风险

### 7.1 关键风险与缓解

| 风险 | 缓解 |
|---|---|
| LLM 短候选准确率不足（P0 头号风险） | 动作缓存 + 置信门控 + 两级 LLM 兜底（短候选不准回退完整生成） |
| 潜空间 rollout 漂移（A1） | 残差 + EMA target + 集成方差截断 + 短时程滚动重规划 |
| 专家卸载磁盘 IO 抖动 | 路由预取 + 4KB 页式 + 热专家 LRU 常驻内存 |
| 结果模型新工具空窗 | 分档（零数据截断 + 试探）+ 快速权重秒级近似 |
| 极端稀疏比不可达 | 渐进扩容，P0 先 1B 验证（稀疏比 ~60 倍），不一步到 10B |
| 冷门工具先验规则失效 | 规则作用可解释槽位层 + 数据校准，失效回退截断 |

### 7.2 与纯 LLM 方案对比预期

| 维度 | 纯 LLM Agent（轻薄本） | JEPA×DSH v3 |
|---|---|---|
| 稳态延迟 | 4–10 s（不可用） | **30–50 ms** |
| 未命中延迟 | 4–10 s | 1–3 s |
| 参数解码成本 | 每次 LLM 生成 JSON | **模板+分类器，多数零 LLM** |
| 幻觉动作风险 | 高 | 低（预测误差实时预警，哨兵形态） |
| 推理能耗 | 高（每次大 LLM 前向） | 低（激活 <100M，CPU 可跑） |
| 知识容量 | 受 LLM 尺寸限制 | **可扩到 10B（磁盘），不涨延迟** |
| 环境动态建模 | 弱 | 中（潜空间世界模型） |

**最终定位**：JEPA 是让 Agent **在轻薄本上变得可行**的必要组件——LLM 短候选 + JEPA 参数解码 + 排序，把 LLM 从"每次完整生成"里解放出来。最坏情况（排序无收益）JEPA 仍以"参数解码 + 世界模型哨兵"立足；最好情况成为"低能耗、大知识容量、可自适应的决策核心 + LLM 兜底"。

---

## 附录 A：假设清单

A1 稀疏预测器潜空间 rollout 稳定性（P1 准入门槛）。
A2 LLM 短候选准确率（P0 头号风险，需最先实测，标准 <80% 则调整）。
A3 动作缓存命中率 >70%、单专家 CPU 前向 0.5–2ms、0.5B int4 LLM 5–15 token/s 均为经验估计，需本机基准测试校准。
A4 视觉分支为可选二期，纯文本为主。

## 附录 B：关键参考文献

- LeCun (2022). *A Path Towards Autonomous Machine Intelligence*. OpenReview.
- Huang, LeCun, Balestriero (2025). *LLM-JEPA*. arXiv:2509.14252.（纯语言 JEPA 依据）
- Assran et al. (2025). *V-JEPA 2*. arXiv:2506.09985.
- Shazeer et al. (2017). *Sparsely-Gated MoE*（专家路由/负载均衡）.
- Lepikhin et al. (2021). *GShard*（专家并行/卸载）.
- Fedus et al. (2022). *Switch Transformer*（top-1 稀疏）.
- Rajbhandari et al. (2020). *ZeRO*（参数分片/卸载到 CPU 内存）.
- Sheng et al. (2023). *FlexGen*（低显存 LLM 推理，CPU/GPU 混合）.
- Yang et al. (2026). *Clin-JEPA*. arXiv:2605.10840.（rollout 稳定性）.
- Cignoni et al. (2025). *CLA*. CoLLAs 2025, arXiv:2507.10434.
