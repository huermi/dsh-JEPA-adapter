# 睡眠巩固验证报告（C5b SleepConsolidation）

> 日期：2026-08-24 ｜ 组件：`components/consolidation.py` ｜ 验证：`sleep_check.py`
> 状态：核心机制 ✅ 实证成立；防遗忘 ✅ 有条件成立（rehearsal 比例是关键）

---

## 一、组件规格

```
SleepConsolidation(epochs=3, batch=32, prio_power=1.0, prio_mix=0.5,
                   lr_scale=0.3, recent_keep=0.2)
consolidate(memory, world_model, rehearsal=None) → {e1_before, e1_after,
    drop_pct, n_replayed, n_rehearsal_sampled, n_forgotten}
```

| 机制 | 参数 | 生物学对应 |
|---|---|---|
| 优先级回放 | prio_power（1.0=按惊讶度加权） | 睡眠选择性强化强烈/新颖经验 |
| 优先级+均匀混合 | prio_mix=0.5（采样分布 = 0.5×均匀 + 0.5×优先级） | 既强化惊奇事件，也巩固常规经验 |
| 低 lr 多轮重放 | lr_scale=0.3 × 在线 lr，epochs 轮 | 纺锤波反复重放，防覆盖旧知识 |
| 短期记忆衰减 | recent_keep=0.2（巩固后保留 20%） | 海马体清空，经验刻进权重 |
| 旧经验重放（防遗忘） | rehearsal 参数（可选，惊讶度动态计算） | 睡眠同时重放新旧记忆 |

前置改动：`memory.transitions` 升级为四元组 `(s, a, s_next, e1)`，`add_transition` 记录完整转移并带惊讶度（睡眠重放的优先级依据）；`replay()` 保持旧接口兼容。

## 二、实验 1：选择性强化（核心机制）

场景：500 条转移 = 480 普通（小偏移）+ 20 稀有重要（大偏移，不同区域，可区分）。白天在线学 100 条 → 睡眠重放 192 条。

| 条件 | 稀有 E1 下降 | 普通 E1 绝对值（初 0.00002） |
|---|---|---|
| 不巩固 | 0% | 0.00002（不变） |
| 均匀重放 | 4% | 0.00004（不变） |
| **混合重放（默认）** | **55%** | 0.00482（仍 <0.02，好预测） |
| 纯优先级（极端） | 77% | 0.01631（接近阈值，边缘） |

**裁决：✅ 选择性强化成立**——混合重放对"稀有重要经验"的掌握是均匀的 14 倍、不巩固的无限倍，且不饿死常规知识。

**关键教训（纯优先级为何不是最优）**：纯优先级（mix=1.0）把采样几乎全部导向高惊讶子分布（123/192 次），模型被拉向稀有偏移，普通知识 E1 从 0.00002 恶化到 0.016（相对 -66320%）。**人类睡眠不是只做噩梦——它同时巩固常规经验（梦大多是平凡的）。mix=0.5 是"选择性但不独占"**。

## 三、实验 2：防灾难性遗忘（rehearsal 比例效应）

场景：学会任务 A（区域 1）→ 白天学任务 B（区域 2）→ 睡眠巩固 B 时混入 A 的旧经验。

| 条件 | 巩固 B 后 A 的退化 | 旧经验采样占比 |
|---|---|---|
| 无旧经验（对照组） | **+255%** | 0/192 |
| 小比例 rehearsal（200 条池） | +244% | 35/192（18%） |
| **大比例 rehearsal（480 条池）** | **+78%** | 104/192（54%） |

**裁决：✅ 有条件成立——rehearsal 防遗忘的生效条件是比例 ≥50%。** 18% 杯水车薪，54% 显著缓解（+255% → +78%）。

**关键发现（连续表征的固有边界）**：动态惊讶度 rehearsal（用当前模型对旧经验的误差作为优先级——"正在被遗忘的记忆优先重放"）方向正确但力量不足。根源是**连续共享表征下梯度学习无法完全隔离"学新破坏旧"**——与 jpi5 的对照：符号层（n 无关特征）天然零遗忘，连续层需要大比例 rehearsal 或 EWC 式正则。**表征分离是防遗忘的正解，rehearsal 是连续表征的缓解剂。**

## 四、使用指引

| 场景 | 配置建议 |
|---|---|
| 日常巩固（任务完成/周期睡眠） | 默认（prio_mix=0.5, lr_scale=0.3, epochs=3） |
| 多任务交替（防遗忘优先） | rehearsal 池 ≥ 新经验同等规模（≥50% 占比） |
| 表征已符号化（结构分离） | rehearsal 可省（jpi5 实证天然零遗忘） |
| 强化优先（重要新经验） | prio_mix 上调至 0.7-0.8，注意常规知识监控 |

## 五、文件

- `components/consolidation.py`：C5b 睡眠巩固组件
- `components/memory.py`：transitions 四元组升级（含 e1）
- `components/system.py`：learn 记录完整转移
- `sleep_check.py`：验证脚本（实验 1+2，可复现）
- 自检：components/self_check.py 全部通过（无回归）
