# JEPA 通往 AI 的路径：相关准备工作全景调研

> 日期：2026-08-24 ｜ 目的：为"五层实践路径"寻找已有人做过的准备，判断每层成熟度与空白。
> 方法：Web 调研 2025-2026 论文 + GitHub + 行业动态，覆盖 8 个方向。

---

## 0. 一句话结论

**五层路径中，三层已被工业级力量推进（身体/世界模型/符号层），一层首次出现实现（时间分层），一层几乎空白（元认知 Configurator）——但没有任何一方在完整走"实践闭环 + 两级能量"这条线。**

---

## 1. 五层路径 × 已有准备对照总表

| 层 | 已有准备 | 成熟度 | 代表工作 |
|---|---|---|---|
| ① 世界/身体（实践场域） | **工业级推进**：AMI Labs（LeCun 离开 Meta 创办）10.3 亿美元种子轮；可穿戴 + 机器人双形态 | ★★★★ | V-JEPA 2-AC；VL-JEPA；冯雁 ICML 2026 演讲 |
| ② 感知编码 fθ | 已解决（表征学习是 JEPA 最强象限） | ★★★★★ | V-JEPA 2（百万小时视频）；LeWorldModel（端到端像素）；VL-JEPA 43× 数据效率 |
| ③ JEPA 世界模型 | 已解决（预测器+能量+规划） | ★★★★ | LeWorldModel（15M 单 GPU）；EB-JEPA（单 GPU 库）；stable-worldmodel |
| ④ 符号层（语言/离散） | **刚开先河**：LLM-JEPA（2025.09）把 JEPA 目标引入语言；VL-JEPA 视觉语言 | ★★ | LLM-JEPA（text/code 多视角）；VL-JEPA（嵌入空间跟踪） |
| ⑤ 元认知 Configurator | **几乎空白**：LeCun 2022 蓝图 + 概念讨论，无成熟实现 | ★ | 无（最大空白） |
| 贯穿·时间分层 | **首次出现实现**：H-JEPA 终于落地 | ★★ | Micro-World Models（2026.06） |
| 贯穿·两级能量/价值 | **理论在动，工程未统一** | ★★ | Curiosity-Critic；Remember to be Curious |
| 贯穿·记忆巩固 | **部分解决**：持久世界模型 + 情景记忆已证明必要 | ★★★ | Remember to be Curious（2026.05） |

---

## 2. 关键事实材料（按层）

### 2.1 世界/身体层 —— 已被资本认可

**AMI Labs（2026.03）**：LeCun 离开 Meta 后创办，种子轮 **10.3 亿美元（估值 35 亿）**，史上最大种子轮之一。四枢纽（巴黎/纽约/蒙特利尔/新加坡），投资人含 Nvidia、Samsung、Toyota Ventures。核心团队：Michael Rabbat（VP World Models）、Saining Xie（CSO）、冯雁（CRO）。首个合作方 Nabla（医疗 AI，FDA 级 agentic 系统）——**明确走"高可靠性世界模型"路线，首年纯研究**。

**冯雁 ICML 2026 演讲**：两大智能体形态——
- 可穿戴辅助型（智能眼镜）：第一人称感知，过程规划，**需要长期+情景记忆做个性化**
- 自主执行型（机器人/自动驾驶）：分层规划，高层指令→子任务→电机动作，需要**关联记忆**

**关键数据**：人类物理推理 64.7% vs Claude 41.2% / GPT-4o 32.4%；PhysBench 人类 95.9% vs 最好 VLM 52%——**落地差距（Grounding Gap）实锤**。

### 2.2 感知编码层 —— 最强象限

- **V-JEPA 2**：100 万+小时视频自监督，SSv2 77.3%、EK100 39.7% R@5。
- **LeWorldModel（2603.19312）**：两 loss 端到端（预测 + SIGReg 高斯正则），15M 参数单 GPU 数小时，规划快 48×。
- **VL-JEPA（2026）**：基于 V-JEPA 2 编码器 + 文本编码器，嵌入空间跟踪动作/状态（非 token 生成），选择性解码 2.85× 提速、参数减半、**数据效率 43×**，CVPR 2026 EgoVis 冠军，开放词汇零样本。

### 2.3 世界模型层 —— 已工程化

- **EB-JEPA（2602.03604）**：单 GPU 库，VICReg 防坍缩，ablation 证明 std/cov/simt/idm 缺一不可。
- **stable-worldmodel（2605.21800）**：统一数据/训练/MPC 评估平台，30+ 环境带 FoV。
- **AdaJEPA（2606.32026）**：测试时自适应，MPC 闭环内 1 步梯度更新，分布偏移下成功率翻倍。

### 2.4 符号层 —— 刚开先河（空白判断需修正）

**LLM-JEPA（2509.14252，Huang/LeCun/Balestriero）**：
- 核心：next-token 损失 + JEPA 损失（从 Text 嵌入预测 Code 嵌入，余弦距离）
- 用"多视角"配对数据（NL↔正则、NL↔SQL、问题↔答案）作为 JEPA 视图对
- **关键发现**：JEPA 项促使模型学到 Text→Code 近似线性映射（奇异值分析证实）；LoRA rank=512 超越全量微调；抗过拟合
- 局限：需配对视图数据；三次前向传播成本翻倍
- **意义**：符号层的第一个实证——不是把文字塞进向量，而是**在嵌入空间预测视图映射**，与"离散标签联觉"直觉兼容

**VL-JEPA**：语言作为查询，嵌入空间跟踪视觉状态，开放词汇。

### 2.5 元认知 Configurator —— 依然空白

- 所有文献/报道中，Configurator 停留在 LeCun 2022 蓝图的"调节各模块参数与注意力"概念。
- 无成熟实现、无开源代码、无论文系统化探索"目标生成 + 能力暴露 + 自指预测"。
- **这是五层中唯一没有工业/学术力量推进的层。** DCA 的 Self-model/InfoDrives 恰好是此空白的民间尝试。

### 2.6 时间分层 —— H-JEPA 首次落地

**Micro-World Models（2026.06，Patil，Zenodo 开源）**：
- 首个真正的 H-JEPA 实现：**无 EMA target**（VICReg 显式防坍缩）；**双尺度时间预测**（浅层短期 + 深层 tap 预测 25 帧后宏观事件）；动作条件前向动力学；CEM 在 256d 潜空间规划；三体碰撞模拟器。
- 复合目标 121→40，方差/协方差诊断非退化，线性探针恢复实体位置。
- **局限**：合成微型世界（三体碰撞），非真实场景；单作者技术报告。

### 2.7 价值/内在动机 —— 理论在动，工程未统一

**Curiosity-Critic（2604.18701）**：
- 质疑局部预测误差好奇心，提出**累积预测误差改进**作为内在奖励 = 当前误差 − 渐近误差基线
- 批评者在线估计基线，**在线区分认识论误差（可还原，值得探索）与偶然误差（不可还原，应放弃）**——最接近"两级能量"的工程化形式
- 随机网格世界超过 prediction-error 和 visitation-count 基线

**Remember to be Curious（2605.22814，Berkeley/Wayve）**：
- 诊断"健忘式探索"：纯预测误差好奇心在复杂 3D 失效——**无空间持久性和情景上下文，agent 反复重访已探索区域获得虚假新奇奖励**
- 解法：在线 3DGS 作为**持久世界模型** + Transformer agent 带**长期线性注意力记忆**
- 纯好奇心训练，零样本泛化；消融证实两组件缺一不可
- **对 DCA 的直接意义**：Playground 探索会遇到同样的"健忘式探索"陷阱——记忆与世界的持久性不是可选优化，是好奇心的必要条件

### 2.8 记忆巩固 —— 与好奇心耦合出现

- Remember to be Curious 的持久世界模型 + 情景记忆 = 探索的必要组件。
- 冯雁演讲明确：可穿戴智能体需要长期+情景记忆做个性化；机器人需要关联记忆。
- JEPA 社区尚无独立的"巩固/睡眠/遗忘"系统性工程工作。

---

## 3. 对你五层路径的修正（交锋）

1. **符号层不是空白，是"刚开先河"**：LLM-JEPA 已证明"嵌入空间视图预测"在语言上有效且映射近似线性。符号层应站在 LLM-JEPA 上，而非从零设计。**警惕**：LLM-JEPA 的视图对需配对数据，对"自组织符号涌现"是约束——你的 Outcome embeddings 原型聚类可能是无配对数据的补充路线。
2. **元认知 Configurator 是唯一真空白**：工业界在做身体/感知/世界模型/符号，学界在做时间分层/好奇心，唯独 Configurator 无人系统化做。**这是唯一没有巨人的层**——既是机会（低竞争），也是风险（无先例可抄、难以验证）。
3. **两级能量已有理论盟友**：Curiosity-Critic 的"累积误差改进 − 基线"就是 E2 的雏形，且它证明**认识论/偶然误差可在线分离**——你的 E2 设计可以直接借用它的基线估计器。
4. **记忆持久性是好奇心的前提（新证据）**：Remember to be Curious 从实证上打击了"纯能量驱动探索"——没有持久世界模型 + 情景记忆，好奇心会退化为循环重访。这强化了"记忆不是附加件"的判断，也预示 DCA 的 Playground 需要先解决持久性。
5. **时间分层已有人趟路**：Micro-World Models 证明 H-JEPA 无需 EMA 也能稳定（VICReg 路线），且双尺度预测可行。你的时间分层不必从零发明，先复现它再扩展。

---

## 4. 结论：站在哪里，攻哪里

| 决策 | 建议 |
|---|---|
| 站在巨人肩上（复用） | 感知编码（V-JEPA 2 权重）、世界模型（LeWorldModel/EB-JEPA）、符号层（LLM-JEPA 目标）、时间分层（Micro-World Models 骨架） |
| 独立攻克（空白） | **元认知 Configurator**：目标生成 + 能力暴露 + 自指预测——无先例，可定义问题 |
| 差异化（理论可借） | 两级能量：借 Curiosity-Critic 的误差基线分离，补上"E2 由 E1 长程积分涌现"的机制 |
| 必须自建（无现成） | 实践闭环本身：没有任何工作在做"预测→行动→观测→重预测"的完整认知体闭环（DCA 的 Sim-Exec 是最接近的民间尝试） |

**一句话**：真正没有巨人、且与你的积累（Self-model/InfoDrives/Sim-Exec/Outcome 原型）最匹配的战场，是**元认知 Configurator + 实践闭环**——前者是空白，后者无人完整做过。其余三层站到巨人肩上即可。
