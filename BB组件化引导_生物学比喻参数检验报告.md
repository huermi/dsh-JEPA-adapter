# BB 组件化引导对照 — 生物学比喻参数检验报告

> 日期：2026-08-24 ｜ 脚本：`bb_component_check.py` ｜ 基于：P0-P3 组件系统（内稳态 C4 / Configurator C10 / 感知-预测-记忆-符号-蒸馏全组件）

> **⚠️ 2026-08-24 晚间修正声明（重要，必读）：**
> 本报告的核心结论（ε-greedy 43.3% > 固定引导 24.0%）建立在**行为伪影注入**上——注入的长机器特征是手拍值（F 全 1.0/0.8），不是真实结构。用**真实结构注入**（真实计数器机器规则表 + 真实特征 + 真实步数）重做后（`bb_human_check.py`）：
> - **ε-greedy 43.3% 是伪影假象**——真实池上仅 24.8% 且命中 0 台长机器
> - **固定引导 24.0% 被低估**——真实池上 50.1%（真实计数器特征与池内短机器部分相关，外推部分有效）
> - **真正正解是环内容分析（反汇编级）**：零模拟静态分析"环在干什么"（写1×移动=扩张），100% 命中全部长机器
> - 本报告的"生物学比喻参数三层结构"分析仍可作为方法论参考，但 hunger_rate=0.05 的数值标定服务于伪影场景，**标定无效**
>
> 详细证据链见《BB人类式反汇编实验与错误结论反思.md》。以下原文保留以供追溯。

---

## 0. 结论摘要

**用组件系统重做 BB 引导，得到一个超出预期的结果：内稳态门控的 ε-greedy（饥饿驱动的探索-利用混合）在结构长尾分布上超过纯预测引导（43.3% vs 24.0%，+19.3pp）。** 这回答了用户的问题——生物学比喻参数在 BB 上**语义保持、量纲重标定、调制自由度重映射**，且内稳态机制在"统计外推失效"的区域提供了纯预测给不了的价值（探索出口）。

> ⚠️ 上述摘要已被修正声明推翻（见文首）：43.3% 是行为伪影假象。

## 1. 实验设计（BB 任务 → 组件系统映射）

| 网格世界（jpi11/12 标定） | BB 任务映射 |
|---|---|
| hunger 驱动"去资源" | hunger 驱动"利用预测"（探索-利用门控） |
| 进食 = 靠近资源 | 进食 = 验证到长运行机器（log > 4.0） |
| 死亡 = 长期饥饿 | 死亡 = 连续 15 次没发现好机器（探索失败期） |
| tick 数千次连续交互 | 预算仅 60 次验证（warm 10 + 引导 50） |

候选池：1500 台 n=4 图灵机（行为指纹 6d）+ 注入 4 台计数器机器（log 8-11，真实长尾）。**注入是必须的**——1500 随机采样最长仅 log 2.64（BB 长尾：长机器指数级稀有），无注入时所有策略都 100%（无挑战）。

三种模式 × hunger_rate 扫描（3 seed 平均）：

| 模式 | hr=0.01 | hr=0.05 | hr=0.10 | hr=0.30 |
|---|---|---|---|---|
| A 固定引导（jpi6 基线） | 24.0% | 24.0% | 24.0% | 24.0% |
| B 内稳态 ε-greedy | 16.7% | **43.3%** | 24.0% | 24.0% |
| C 内稳态 + Configurator | 16.7% | 43.3% | 24.0% | 24.0% |

## 2. 四个生物学比喻参数检验结论

### ① 内稳态比喻语义：成立，且贡献超出预期（+19.3pp）
饥饿（hunger）作为"探索-利用节流阀"在 BB 上有效——hr=0.05 时探索 31 / 利用 18 的混合策略效率 43.3%，远高于固定引导的纯利用（24.0%）。**机制是"饥饿门控的 ε-greedy"而非"进食反馈"**（本次 feed 事件为 0——好机器太稀，验证不到）。

**为什么探索优于纯预测引导（BB 的核心洞察）**：固定引导 50 次全用预测 top，但注入长机器的特征极端（全 1.0），warm 10 个样本没学到"极端特征→长运行"的模式 → 预测器外推给低分 → 永远不选 → 盲区。**随机探索是逃离"预测器外推盲区"的唯一出口**。这在可学习分布（网格）上不明显（固定引导也行），但在结构长尾（BB）上是关键差异——**内稳态机制的价值在"统计失效的区域"才显现**。

### ② hunger_rate 量纲：必须按"决策次数"重标定
- 网格世界最优：0.001-0.03（数千 tick 连续交互，hunger 缓慢积累）
- BB 最优：**0.05**（60 次预算，hunger 需要 ~20 次验证内满一次来驱动探索-利用循环）
- 差异 ~2-50 倍，本质是**时间尺度不同**：网格 tick = 连续控制步，BB tick = 一次昂贵的验证。比喻语义（饥饿节奏驱动行为）保持，数值必须重标定。

### ③ 调制自由度：从"内稳态速率"重映射到"探索温度"
- 网格世界：Configurator 调 hunger_rate（连续动力学参数）→ 有效（jpi12）
- BB：关键自由度是**探索温度（ε）**而非 hunger_rate——hr 只间接影响探索-利用平衡
- 死亡时探索温度 +0.3（疼痛→探索新方向）、进食时 -0.15（奖励→利用）——**"疼痛→改变策略"的语义在 BB 上是正确的映射**（jpi11 的"死亡急刹车"在 BB 等价于"探索失败→换方向"）

### ④ Configurator 在 BB 失效（诚实记录）：稀疏反馈信号缺失
B = C（Configurator 无增益）——原因：feed 事件 0 次，绩效窗口（平均 log）一直低平（~1-2），**Configurator 的"水平加速/爬山"需要绩效有信息量**，BB 的稀疏长尾让绩效信号长期无变化 → 无调制信息。这与 jpi10 的 STM-Critic 稀疏奖励结论一致：**极稀疏奖励下即时反馈机制失效**（需要记忆回放/结构理解补偿）。

## 3. 与网格世界的系统对比

| 维度 | 网格世界（jpi11/12） | BB（本轮） |
|---|---|---|
| 内稳态机制 | 饥饿→去资源，进食=满足 | 饥饿→利用预测，进食=发现长机器 |
| 最优 hunger_rate | 0.001-0.03 | **0.05**（量纲重标定） |
| Configurator | ✅ +2.97pp（可学习漂移） | ❌ +0.0pp（稀疏反馈无信号） |
| 内稳态 vs 固定 | 20.8% vs 29.1% WAIT（+8.3pp） | **43.3% vs 24.0%（+19.3pp）** |
| 关键价值场景 | 防死寂（静态世界） | 逃离预测外推盲区（结构长尾） |

## 4. 组件化重做 BB 的价值

1. **端到端检验**：P0-P3 的组件系统（内稳态 C4 + Configurator C10 + 感知/预测/记忆/符号/蒸馏）成功适配到完全不同的问题结构（从连续控制到离散搜索）——**组件接口的"任务无关性"得到验证**
2. **重现并深化 jpi3/9 结论**：统计引导对新颖结构的外推盲区（24.0% ≈ jpi9 的 23.1%），但新增发现——**内稳态驱动的探索提供逃离盲区的机制**（+19.3pp）
3. **参数检验落地**：生物学比喻的"语义-量纲-自由度"三层结构被实证——语义可迁移、量纲必重标、自由度需按问题重映射

## 5. 下一步

- **蒸馏接入**：hr=0.05 的 43.3% 引导经验 → 蒸馏快策略（探索-利用门控的编译），预期保留大部分收益
- **符号层辅助**：注入长机器的极端指纹形成"长运行符号"原型 → 引导时偏好与符号相似的机器（结构理解的统计近似）
- **稀疏反馈补偿**：记忆回放（jpi10 教训）——把罕见的"进食"事件存入记忆反复重放，给 Configurator 提供信号
