# 人类式反汇编实验 + 错误实验结论反思

日期：2026-08-24
状态：实验完成（决定性结果）+ 系统性反思

## 一、实验：人类式方法在真实结构长尾上 vs 全部统计方法

### 设计修正（关键）
之前所有 BB 注入实验（jpi9、bb_component_check、bb_uncertainty_check）都是**行为伪影注入**——手拍特征值（F 全 1.0/0.8）。本轮改为**结构真实注入**：真实计数器机器（make_counter_machine 规则表）+ 真实运行步数（超 20 万步不停机 → log 11.51）+ 真实 extract_symbols 特征。池：1500 台随机 n=4 机器（真实最长 log 2.89）+ 4 台计数器。预算 60 次验证，3 seed。

### 结果

| 模式 | 方法 | 效率 | 长机器命中 |
|---|---|---|---|
| A fixed | 全池行为指纹回归 | 50.1% | 0 台 |
| B egreedy | 行为指纹 + ε-greedy 内稳态 | 24.8% | 0 台 |
| C human_static | 纯图论预筛（有环+SCC+少出口） | **0.0%** | 0 台 |
| **C2 human_v2** | **环内容分析（反汇编级）** | **100.0%** | **4/4 台** |
| D human+reg | 静态预筛 top200 + 子集回归 | 75.0% | 2 台 |

关键诊断：注入机器在图论排序中排名 **86-151**（纯图论抓不到），在环内容排序中排名 **6-9**（直接命中前 60）。

### 为什么环内容分析是正解（人类到底在看什么）

- 天真图论（有环→高分）失败：池中 813 台不停机机器里，大量"强连通+无出口"的空转机器（静态分数 1.0，实际 log 0）——**静态结构无法区分"计数器"和"空转"，两者图论同构**
- 人类看规则表会追问"这个环在干什么"：计数器环内规则全部"写1+移动"（磁带扩张）；空转机器环内"写0原地"（死循环）
- **环内容（写1比例 × 移动比例）是零模拟可判的，且是长尾机器的判别特征**——这就是"反汇编"：不只是看结构，是读环的语义

### 方法优劣完整排序（真实结构池）

**环内容分析 (100%) > 预筛+回归组合 (75%) > 全池回归 (50%) > 随机探索 (25%) > 天真图论 (0%)**

## 二、反思：哪些构造建立在错误的实验结论上

### 最大污染源：行为伪影注入（手拍特征值）

jpi9 / bb_component_check 的注入是"伪造行为维"（F 直接置 1.0/0.8），不是真实计数器机器。这让所有方法在与"统计上突兀但结构上无意义"的假目标搏斗，系统性扭曲了方法比较：

| # | 被污染的结论 | 伪影池上的结果 | 真实结构池的真相 |
|---|---|---|---|
| 1 | "ε-greedy 43.3% 是结构长尾的有效机制" | 43.3%（手拍极端值让随机采样碰到就中） | **24.8% 且命中 0 台——随机探索无效** |
| 2 | "统计回归在 BB 上系统性失效（24%）" | 24.0%（全 1.0 极端特征切断外推） | **50.1%（真实计数器特征与池内短机器部分相关，外推部分有效）** |
| 3 | "行为指纹 > 静态结构"（jpi6 +20.1 vs +19.3） | 无注入池（真实最长 log 2.9，全是短机器） | **结构长尾上静态环内容 100% >> 行为指纹回归 50%**——jpi6 裁决适用范围被严重误读 |
| 4 | "BB 的符号层 = 行为指纹" | 行为指纹在伪影池胜出 | 真实符号层内容应是"环内容分析（反汇编）"，行为指纹只是统计可学习分布的信号 |
| 5 | "Configurator 在 BB 上无增益（B=C，绩效信号缺失）" | feed 事件 0 次 | 真实池上回归可达 50% 时 feed 事件存在，绩效信号并非完全缺失；但 ε-greedy 命中 0，门控本身在真实池无效 |
| 6 | "BB hunger_rate 最优 = 0.05" | 伪影池标定 | 服务于一个错误的场景，标定意义存疑 |
| 7 | "不确定度驱动探索失败（LeCun 药方裁决）" | 伪影池上做的 | "统计不寻常 vs 结构特殊"正交性结论仍成立（图论 top 全是 log 0 是同一现象的另一面），但 LeCun 药方在真实池上未测——诚实标注为开放问题 |

### 未被推翻的结论（值得保留）

- **"BB 需要符号层/结构理解"（jpi3 核心洞察）依然成立**——但符号层的正确内容被我们搞错了：不是行为指纹，是环内容分析
- **"结构长尾无法纯统计解决"方向正确**——程度错了（回归 50% 不是 24%），且统计+结构组合（D=75%）优于纯统计
- **蒸馏（+59.4pp）与持续学习（零遗忘+正迁移）结论**——不依赖注入方式，不受污染

### 元教训（方法论级）

**实验场景的构造方式（注入方式）决定了结论的有效性。** 行为伪影注入（手拍特征）制造了"统计突兀假目标"，让所有方法比较都失效；真实结构注入（真实规则表+真实特征+真实步数）才是有效的实验场。这补充进 simulation-validation 技能：**"场景有效性验证"必须包含"注入物必须是真实对象，不能是伪造特征"**——伪影注入会让负结果看起来像方法失败、让正结果看起来像方法有效，而实际上都是场景假象。

## 三、改进结论（落进架构）

1. **C8 符号层分层**：L0 图论粗筛（有环/SCC/可达，缩搜索空间）→ L1 环内容分析（写1×移动 = 扩张判别，结构长尾正解）→ L2 行为指纹（统计可学习分布信号）。当前实现只有 L2 的雏形，缺 L0/L1。
2. **BB 引导机制**：替换"行为指纹回归 + ε-greedy"为"静态粗筛 → 环内容排序 → 预算花在 top K 子集"（C2/D 的形态），零模拟预筛 + 少量验证。
3. **双探索接口保留**（可学习分布 vs 结构长尾），但结构长尾侧的机制从"随机兜底"升级为"环内容判别"。
4. **所有依赖伪影池的数值标定（hunger_rate=0.05 等）标记为无效**，待真实池重标。

## 四、一句话

**人类式反汇编（看环在干什么，零模拟）在真实结构长尾上 100% 找到全部长机器，碾压一切统计方法——而这恰恰是因为它读取了"环的语义"而不是"环的存在"。我们之前的行为伪影注入实验，让 ε-greedy 的 43.3%、回归的 24%、行为指纹>静态结构 全部建立在假目标上；真实结构池揭示的排序是：环内容分析 > 预筛+回归 > 回归 > 随机 > 天真图论。符号层的正确内容不是行为指纹，是反汇编级结构语义。**
