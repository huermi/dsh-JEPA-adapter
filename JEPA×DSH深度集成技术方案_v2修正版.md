# JEPA × DeepSeek Harness（DSH）深度集成技术方案（修正版 v2）

> 产出方式：四位 AI 架构专家辩论委员会，经 **8 轮** 严格辩论，逐条修正 v1 的 6 项缺陷。
> 参与者：**架构师（Architect）**、**JEPA 专家（JEPA-Specialist）**、**DSH 专家（DSH-Specialist）**、**质疑者（Skeptic）**。
> 日期：2026-08-18 ｜ v1 缺陷来自上一轮《JEPA×DSH深度集成技术方案_辩论委员会.md》。

---

## 0. v1 缺陷 → v2 修正对照表（本文档的产出契约）

| # | v1 缺陷 | v2 修正 | 落点章节 |
|---|---|---|---|
| D1 | 稠密 backbone，稀疏激活/层级抽象未落地 | **稀疏层级状态编码器**：字段级增量编码 + 原型门控 + 高层优先，低层仅误差大时触发 | §2.1、辩论第2轮 |
| D2 | 默认视觉观察，跨模态对齐假设过高 | **默认纯文本/JSON**，状态编码器输入为 JSON 字段；视觉仅作可选扩展并给出迁移路径 | §1、§2.1、辩论第1轮 |
| D3 | LLM 生成参数 JSON 延迟/错误率被低估 | **模板填充 + 轻量分类器优先**，LLM 仅处理自由文本参数（占比极小） | §3、辩论第4轮 |
| D4 | T2 触发迟钝，适应不敏捷 | **秒级适应**：上下文校准 + 在线原型更新 + 快速权重，梯度级异步更新，非单一触发式 | §4、辩论第7轮 |
| D5 | 概念空间/高层 H-JEPA 空中楼阁 | **分层规划暂缓**：MVP 不含自动概念涌现，长时程由 LLM 显式生成子目标 | §6、辩论第6/8轮 |
| D6 | 定位不渐进 | **渐进式路线**：P0 = JEPA 预测辅助（LLM 生成候选，JEPA 排序后果） | §6、辩论第8轮 |

### 0.1 硬性约束与假设

| # | 约束 | 内容 |
|---|---|---|
| H1 | 观察模态 | **默认纯文本/结构化 JSON**，不依赖视觉；视觉为可选扩展 |
| H2 | 显存 | 总显存 ≤ 20 GB（本文方案峰值 < 6.5 GB） |
| H3 | 延迟 | 单步推理 ≤ 1.5 s（本文方案默认 ~20–60 ms，触发 LLM ~200 ms） |
| H4 | 参数解码 | 模板 + 分类器优先，LLM 仅自由文本参数 |
| H5 | 分层规划 | MVP 不包含自动概念涌现/高层 H-JEPA |
| A1 | 未验证 | 稀疏预测器在潜空间 rollout 的稳定性（P1 准入门槛） |
| A2 | 可选二期 | 视觉分支、预测器更多专家、可学习可塑性门控 |

---

# 第一部分：八轮辩论记录

---

## 第 1 轮：总体架构与数据流（纯文本/JSON 场景）

**架构师**：JEPA 仍定位为新 Agent Loop 插件，但接口从 v1 的"潜状态进→潜状态出"进一步收紧为 `tick(goal_latent, state_latent, candidates) -> (chosen_action, confidence)`。关键变化：**Loop 的输入里多了一项 `candidates`（LLM 预生成的候选动作）**，因为 P0 阶段 JEPA 是"预测辅助"，不独立采样动作。双总线在纯文本场景下合并为一个"文本总线 + 其上的潜空间视图"，潜空间不再是一条物理独立的管道，而是文本总线每一条消息的**伴随潜表示**（sidecar）。

**JEPA 专家**：必须纠正一个根本误解——JEPA 的价值**从来不依赖视觉**。JEPA 的定义是"在表征空间预测，而非在信号空间（像素/词元）预测"。纯文本场景里，状态编码器把 JSON 压缩成语义潜向量，预测器在潜空间预测下一状态，这就是 JEPA。**LLM-JEPA（Huang, LeCun, Balestriero 2025）已经证明了这套在纯语言上是可行的**：它不预测下一个 token，而是预测 [PRED] token 位置上的连续嵌入。本方案的状态编码器就是 LLM-JEPA 的"编码器"，预测器就是它的"[PRED] 预测头"。

**DSH 专家**：工程上坚持两点：潜表示作为**轨迹日志的 sidecar 字段**（每条消息附带 `latent` 键），这样离线采集、版本回滚都走 DSH 现有机制，不加新存储。Loop 接口要能**优雅降级**：当 `confidence < τ` 时，返回空动作并声明"交由 LLM"，而不是硬着头皮输出一个差动作。

**质疑者（四问）**

> **Q1** 纯文本场景下"表征空间"和"词元空间"的界限在哪？如果状态编码器就是一个小 LLM 的隐藏态，那"预测下一个隐藏态"和"预测下一个 token"有什么区别？JEPA 的价值主张还剩多少，还是说你们只是在给 LLM 的 hidden state 做预测、换了个名字？
> **Q2** 双总线在纯文本下几乎重合，v1 的"双总线 + LLM 唯一翻译器"是不是退化成过度设计？潜空间 sidecar 每次都要文本↔潜来回转，转换开销和误差累积谁负责？
> **Q3** `candidates` 这个输入是谁、什么时候产生？如果每步都要 LLM 先生成候选动作，那延迟上 LLM 依然是瓶颈，JEPA 只是事后排序，这算什么"取代 LLM"？
> **Q4** 状态/动作/目标维度 512/64/256 从哪来？纯文本场景状态真的需要 512 维吗？给不出依据就是拍脑袋。

**回应与修正**

- 对 Q1：**边界在于"预测对象"与"训练目标"**。预测下一个 token 是预测离散词元分布（softmax over 词表，几十万类），且自回归生成；LLM-JEPA/本方案是**预测连续潜向量的值**（回归，无词表 softmax，非生成）。训练信号是 `‖ŝ_{t+1} − sg(s_{t+1})‖²`，不是交叉熵。这带来三个 JEPA 独有的性质：① 编码器可丢弃"不可预测细节"（只保可预测结构）；② 无逐 token 生成，rollout 在潜空间一步到位（便宜）；③ 预测误差 E 天然是"世界模型校准信号"。**所以它不是换名字，是换了训练目标和推理方式**。
- 对 Q2：**承认 v1 双总线在纯文本下冗余，合并为"文本总线 + 潜 sidecar"**。sidecar 是惰性的：只有进入 JEPA-Loop 的消息才计算潜表示，普通插件消息不附带。转换开销因此只发生在决策边界，不在全链路。误差累积靠**唯一写入方**（只有 StateEncoder 写潜，下游只读）控制，避免多轮往返。
- 对 Q3：**承认这是 P0 的本质**。P0 阶段 LLM 仍是决策主体，JEPA 是"后果预测器/排序器"，`candidates` 由 LLM 在**同一次 decode 里**生成（LLM 输出 top-K 个工具调用候选 + 参数，一次前向），JEPA 对 K 个候选做后果预测排序。所以**不增加一次额外的 LLM 调用**，延迟增量只有 JEPA 的排序开销。到了 P3+ 才由 JEPA 独立采样动作，那时 `candidates` 参数留空。
- 对 Q4：**维度下调并给出依据**。纯文本 JSON 状态的信息量远低于视觉，维度砍半：状态潜 `s∈ℝ^128`（字段级 `d=256` 聚合而来）、动作潜 `a∈ℝ^64`、目标潜 `g∈ℝ^128`。依据：LLM-JEPA 的隐藏态 768 维是过完备的，任务可预测结构实际落在更低维流形；128 维足够承载"几十个 JSON 字段的状态转移"，且 128 维让 MoE 预测器极轻。**所有维度默认值，需 P1 实验校正**。

**本轮结论**
1. 纯文本下 JEPA 依然成立：预测连续潜向量而非词元，价值主张不变（LLM-JEPA 为证）。
2. 双总线合并为"文本总线 + 潜 sidecar"，惰性计算、唯一写入方。
3. Loop 接口含 `candidates`（P0 预测辅助），P3+ 留空由 JEPA 独立采样。
4. 维度下调：`s=128, a=64, g=128`，字段级 256。

---

## 第 2 轮：状态编码与潜空间抽象（稀疏层级）

**JEPA 专家**：稀疏层级状态编码器分三层，维度自上而下收敛：
- **L2 全局状态潜** `s∈ℝ^128`：规划器直接使用的紧凑状态，2 层 MLP。
- **L1 字段级表示** `f_i∈ℝ^256`：每个 JSON 字段一个向量，Transformer 聚合层。
- **L0 文本编码器**：冻结的 ~60M 编码器（MiniLM/BGE-small 量级），token → 字段内 token 表示。

**稀疏化的关键不是 MoE，而是"增量编码 + 字段原型缓存"**：Agent 场景里工具调用后 JSON 状态**只有局部字段变化**（人也不会每次重新感知整个场景）。因此每个字段编码结果进一个**字段原型库**，未变化的字段直接取缓存向量，只重编码变化的字段。稀疏激活 = 激活参数量随"变化字段数"而非"状态总字段数"增长。

**架构师**：高层优先原则：默认只用 L2（128 维）+ 变化字段的 L1 缓存；只有当高层预测误差 E 超过阈值，才回溯触发 L0 全量重编码，把细节信息补进来。这是一个**由预测误差驱动的、自底向上的注意力重分配**，等价于"人只在意外时细看"。

**DSH 专家**：JSON 天然有字段边界，按字段切分编码零成本。字段原型库用一个固定容量（1024 槽）的 EMA 更新缓存实现，不做独立向量数据库——延迟可控，KV 语义。

**质疑者（四问）**

> **Q1** 逻辑悖论：你说"高层误差大才触发低层"，但误差是在**高层**算的，高层已经把低层细节丢掉了。用一个丢掉了细节的表示算出的误差，怎么判断"该不该补细节"？这像是用近视眼判断"我是不是该戴眼镜"。
> **Q2** 字段级原型缓存：字段变了才重编码，但"字段是否变化"怎么判定？字符串 hash？那值变了但语义没变（"2026-08-18" vs "2026/08/18"）算不算变？缓存命中率到底多少，能不能给个经验数字？
> **Q3** L0 是 60M 的 dense 编码器，你声称"稀疏激活"，但 L0 一旦触发就是 60M 全量前向，稀疏在哪？总激活参数量到底多少？
> **Q4** 原型库 1024 槽，字段可能上千种（不同工具不同 schema），1024 槽怎么覆盖？满了怎么办？

**回应与修正**

- 对 Q1：**承认逻辑漏洞，修正判定信号**。不能用高层的 E 直接判断，改用**双层信号**：① 高层 E（全局预测误差，粗粒度"世界没按预期走"）+ ② **路由/门控置信度**（L1 聚合时每个字段的注意力熵，熵高 = 该字段表示不确定）。二者之一超阈 → 触发低层重编码。更关键的是，**L2 的输入本来就带一个"残差不确定度"通道**（L1 聚合时输出 mean + var，var 就是"这状态还有多少没被 128 维抓住"），用 var 驱动低层触发，逻辑自洽。
- 对 Q2：**字段变化判定用"语义 hash"而非字符串 hash**——即字段编码后的向量与缓存向量的余弦距离 > ε 才重编码（值变但语义近则不重编）。经验命中率：Agent 轨迹里相邻步的字段变化率通常 < 30%，稳定期 < 10%，因此缓存命中率预期 70–90%（需 P1 实测，A1）。
- 对 Q3：**明确激活量**。默认路径：L2(2M) + 变化字段的 L1 聚合(~3M) ≈ **激活 5M**；触发 L0 时：L0(60M) 但**只对变化的字段跑**（假设变化 20% 字段 → 实际激活 ~12M 等效 FLOPs）+ L1/L2 ≈ **激活 ~17M**。**稀疏比 = 激活/总参数 ≈ 8%–28%**，总编码器参数 ~62M。稀疏不是"L0 永不跑"，而是"L0 只在需要细节时、且只对需要的字段跑"。
- 对 Q4：**原型库按"工具/schema 分片"**：每个工具的字段集有自己的原型槽（每工具 ~16–64 槽），总槽数 = 工具数 × 平均字段数，1024 是**活跃热槽**上限（LRU + EMA 淘汰冷槽）。字段是 schema 绑定的，不会无限增长。

**本轮结论**
1. 三层编码器：L0 文本(60M, 冻结) / L1 字段(256d) / L2 状态(128d)。
2. 稀疏靠"增量编码 + 字段原型缓存"，激活随变化字段数增长；低层触发由"全局 E + 字段注意力熵 + L1 残差方差"三信号驱动。
3. 激活量：默认 ~5M，触发低层 ~17M；总编码器 ~62M。
4. 原型库按工具分片，热槽 1024，LRU+EMA 淘汰。

---

## 第 3 轮：世界模型设计（稀疏专家混合预测器）

**JEPA 专家**：预测器采用**稀疏 MoE**。输入 `(s_t∈128, a_t∈64)` 拼接 → 路由 → top-2 专家 → 输出 `Δŝ∈128`（预测状态残差，而非绝对状态，抑制坍缩，沿用 CL-JEPA 的 Δz 残差模式）。规格：
- 专家数 E=8，每个专家 = 2 层 MLP（192→256→128），~50K 参数。
- 路由网络：1 层线性 + softmax，输出 top-2，~16K 参数。
- 总预测器 ≈ 8×50K + 16K ≈ **0.42M 参数**；每步激活 top-2 ≈ **100K 参数**。

**架构师**：为什么敢让预测器这么小？因为**预测的是 128 维潜的残差，不是原始信号**。128 维已经是高度抽象的压缩态，预测它的转移不需要大模型。这是 JEPA"预测表征而非信号"的直接红利——预测器可以小到可以跑成千上万次 rollout。

**DSH 专家**：路由训练要防专家坍缩（所有 token 挤到 1-2 个专家）。用 MoE 标准配件：**auxiliary load-balancing loss**（鼓励均匀使用专家）+ **noise 注入**（训练时给路由 logits 加高斯噪声，防过早固化）。

**质疑者（四问）**

> **Q1** 8 专家 × 50K = 0.4M 参数，这么小的模型能学出什么"世界模型"？它学到的怕不是状态转移的**一阶线性近似**，和"线性动力学"没本质区别，凭什么叫世界模型？
> **Q2** 路由在 rollout 过程中会变吗？自回归 rollout 16 步，每步路由不同，路由的抖动会不会给 rollout 引入额外噪声，导致漂移比 dense 预测器更严重？
> **Q3** 稀疏化的真实收益：路由网络本身要算、所有专家的路由 logits 都要算，top-2 之外的 6 个专家虽然不激活但也占显存。**总显存没省多少，激活 FLOPs 才省了 6/8**，这点收益值得引入 MoE 的复杂度吗？
> **Q4** rollout 稳定性（v1 就标记的 A1 风险）：潜空间自回归 16 步，误差累积会不会发散？MoE 是不是反而放大了漂移？

**回应与修正**

- 对 Q1：**承认"世界模型"一词要降格**。0.4M 的预测器学的是**局部潜动力学的一阶/二阶近似**，不是完整世界模型。但这对 MPC 是够的——MPC 本质是"短时程局部线性化 + 滚动重规划"，不需要全局精确模型（这是 MPC 的标准假设，机器人领域用局部线性模型做 MPC 是成熟做法）。若实测预测能力不足，**升级路径明确**：专家数 8→32、隐藏 256→512、输入加 4 步历史（128→128×4）。A1 门槛就在这验证。
- 对 Q2：**修正：路由做"轨迹内一致"约束**。训练时对同一 rollout 的相邻步加一个**路由一致性正则**（鼓励相邻步选相同专家），减少 rollout 中的路由抖动。推理时可选"第一步定路由、后续步固定专家"的省算力模式（牺牲精度换稳定性，作为开关）。
- 对 Q3：**承认收益被高估，重新算账**。0.42M 的预测器本身已经小到"稀疏化省不了多少"——这恰恰说明：**预测器的稀疏 MoE 不是为省显存，而是为省 rollout 的 FLOPs 并保留容量扩展空间**。真正的稀疏收益在**状态编码器**（62M，默认只激活 5M）那一侧。所以预测器 MoE 的定位是"**容量可扩展的、抗坍缩的 rollout 引擎**"，稀疏是次要收益。若 P1 证明 dense 单预测器足够，可降级为 dense（简化实现），MoE 作为容量扩展的预留。
- 对 Q4：**漂移控制四件套**（Clin-JEPA 思想 + 残差模式）：① 预测残差 Δs 而非绝对 s（残差天然有界）；② **target 编码器 EMA**（f_ξ 提供稳定的预测目标，stop-grad）；③ 集成（4 个预测器，输出均值 + 方差，方差即截断信号）；④ rollout 中**方差超阈即截断**该分支（不确定就不继续滚）。集成方差还顺带回答了第 6 轮"不确定度从哪来"。

**本轮结论**
1. 预测器 = 稀疏 MoE：8 专家（各 50K）+ top-2 路由，总 0.42M，激活 100K。
2. 预测器小的依据 = 预测 128 维潜残差；"世界模型"降格为"局部潜动力学近似"，MPC 够用。
3. 稀疏收益主要在状态编码器侧，预测器 MoE 是容量预留 + 抗坍缩。
4. 漂移控制 = 残差模式 + EMA target + 集成方差截断 + 路由一致性正则。

---

## 第 4 轮：动作对齐与工具调用（模板优先）

**JEPA 专家**：动作码本同 v1：actor 输出 `a∈ℝ^64` → 最近邻工具码 → 绑定工具。但**参数生成彻底改**：不再让 LLM 生成参数 JSON（D3 缺陷），改为**模板 + 分类器优先**。

**架构师**：参数分三类，处理策略不同：
1. **枚举/布尔/数值参数**：轻量分类器/回归头（每个工具的每个此类槽位一个小 head），从 `a` 和状态上下文直接分类。
2. **固定结构子对象**：递归套用模板（子对象里还是枚举/布尔/数值）。
3. **自由文本参数**（邮件正文、搜索词、代码片段）：**仅这类才调用 LLM**，且给的是"槽位已定位 + 上下文"，LLM 只填空，不做整体 JSON。

**DSH 专家**：模板从 DSH 插件的 **schema 自动生成**（工具注册时从 JSON Schema 推导槽位分类器和模板骨架），新工具零手工成本。schema 里 `enum` 字段 → 分类器；`string` 且无 format → 自由文本（LLM）；`number/boolean` → 回归/二分类。

**质疑者（四问）**

> **Q1** 模板 + 分类器能覆盖多少真实工具？现实里工具参数大量是自由文本——"发送邮件"的正文、"搜索"的 query、"写代码"的 code。模板优先是不是只对"玩具工具"有效，真实占比多少？
> **Q2** 分类器每工具每槽一个 head？工具无限增长，head 无限增长？还是共享？共享的话不同工具的枚举语义不同，怎么共享？
> **Q3** 给"发送 HTTP 请求"的具体流程和延迟。它的 body 可以是 JSON、可以是自由文本，method 是枚举，header 是 kv 对——这三类都碰上了，你逐类给方案和延迟。
> **Q4** 新工具加入，码本和模板快速扩展——模板从 schema 自动生成我接受，但**分类器要训练**吧？新工具的枚举值分类器没训过，零样本怎么分类？

**回应与修正**

- 对 Q1：**承认占比要数据说话，但给出机制上的硬保证**。schema 驱动的分类：`enum/boolean/number` 走分类器，`string` 走 LLM。典型工具（检索、HTTP、文件、数据库）的枚举+数值参数占比通常 > 70%（method、字段名、limit、排序、开关等），自由文本是少数。**机制保证**：凡是 schema 声明 `enum` 的必然走分类器；只有 `format: free-text` 才走 LLM。若某工具自由文本占比高，它天然就该让 LLM 主理——这正是渐进式路线的正确分工，不硬扛。
- 对 Q2：**共享骨干 + 工具专属轻 head + 原型化分类**。分类器不是"每槽独立 MLP"，而是：共享一个 `a → 槽位选择注意力`（决定"填哪个槽"），枚举值分类用**槽位原型库**（每个枚举值一个 64 维原型，最近邻分类）。新工具的枚举值 = 用 LLM 编码枚举值语义 → 生成原型向量 → 加入槽位原型库，**零训练分类**（最近邻）。这和第 2 轮"字段原型缓存"是同一套机制复用。
- 对 Q3：**"发送 HTTP 请求"逐类拆解**（以 `method/url/headers/body` 为例）：

  | 参数 | 类型 | 解码方式 | 延迟 |
  |---|---|---|---|
  | `method`（GET/POST/…） | 枚举 | 最近邻原型分类 | <1 ms |
  | `url` | 结构化字符串（从状态上下文取） | 模板槽位填充（从 JSON 状态字段直接取值） | <1 ms |
  | `headers` | 固定 kv 对 | 模板 + 每值分类/拷贝 | <1 ms |
  | `body` | 自由文本/嵌套 JSON | 仅此触发 LLM | 50–150 ms |

  **结论**：常规 HTTP 请求（body 为空或从状态拷贝）总解码 <5 ms；只有含自由文本 body 时才 +50–150 ms。这正是"模板优先"的收益：**大多数工具调用零 LLM**。
- 对 Q4：**零训练扩展 = 原型最近邻**。新工具注册 → schema 解析 → 枚举值用 LLM 编码成原型（一次性，离线）→ 分类即最近邻。无需训练。唯一需要训练的是"槽位选择注意力"（决定填哪个槽），但它是**共享的、对"哪些槽需要填"这个通用任务**训练的，新工具的槽位选择靠通用能力泛化，不够时用少样本校准（几十条示例微调注意力，秒级，见第 7 轮）。

**本轮结论**
1. 参数解码 = 模板 + 分类器（最近邻原型）优先，LLM 仅 `format: free-text`。
2. 分类器 = 共享槽位注意力 + 槽位原型库（最近邻），新工具零训练扩展。
3. HTTP 请求逐类延迟：常规 <5 ms，含自由文本 +50–150 ms。
4. 模板从 schema 自动生成，无手工成本。

---

## 第 5 轮：目标编码与任务理解（原型检索 + OOD 检测）

**JEPA 专家**：纯文本目标 → LLM 编码 → 投影层 → `g∈ℝ^128`。目标映射器训练 = InfoNCE 对比（正对：目标描述↔成功轨迹终态潜；负对：无关终态）。

**架构师**：在线推理时**目标原型库实时介入**（类人脑记忆，H6 硬要求）：`g = α·投影(文本) + (1−α)·kNN插值(目标原型库)`。α 由"最近原型距离"自适应——原型近则多信原型，远则多信投影。

**DSH 专家**：冷启动 bootstrap 同 v1：LLM 影子规划积累 `(goal, outcome)` 对，500–2000 条后离线训投影层。

**质疑者（四问）**

> **Q1** 对比学习的负例怎么选？随机负例在"目标-终态"这种长文本里会引入大量**假负例**（两个不同的目标其实终态相似），InfoNCE 会学坏。这个硬伤怎么解决？
> **Q2** OOD 阈值 τ 用验证集 tune 出来，部署后分布漂移，τ 就失效了。阈值要不要在线自适应？怎么自适应？
> **Q3** 原型库冷启动是空的，kNN 插值初期就是摆设，那"在线实时使用原型"这个硬要求，初期怎么满足？是不是又要等数据？
> **Q4** 降级策略：澄清 vs 回退 LLM，谁来判定"澄清有效"？用户不回答/回答仍 OOD 怎么办？死循环风险。

**回应与修正**

- 对 Q1：**修正负例采样：困难负例挖掘（hard negative mining）**。不用纯随机负例，而是：① 同批次内其他目标作为负例；② **终态距离近但语义不同的目标**作为困难负例（用终态潜的 kNN 找"终态像但目标不像"的对）；③ 用目标原型库中**不同簇、但投影相近**的原型作负例。这三点把假负例率压下来。
- 对 Q2：**τ 在线自适应**：`τ(t) = 历史 conf 的分位数（如 10% 分位）`，随部署数据滑动更新（EMA），不再用固定验证集值。OOD 判定 = `conf < τ(t)`，天然随分布漂移。
- 对 Q3：**冷启动用"投影-only + 低 τ + 快速建库"**。初期 α 退化为投影-only（无原型可用），但**每条成功轨迹立即写入原型库**（EMA 在线建库），几十条后 kNN 即生效。同时用 LLM 的语义相似度作为**临时原型**（把历史目标的文本嵌入直接当原型向量），让"在线原型使用"从第一天就能跑，不空等。
- 对 Q4：**降级加超时与单向门**。澄清最多 1 轮（避免死循环），用户不答 → 默认回退 LLM 直接规划；回答后仍 OOD → 回退 LLM 并**记录该目标为"已知难例"**，下次遇到同类目标直接走 LLM（学习性降级）。判定"澄清有效" = 用户回答后重编码的 conf 超过 τ，否则无效。

**本轮结论**
1. 目标映射器 = 对比学习 + 困难负例挖掘（防假负例）。
2. 目标原型库在线实时介入（kNN 插值 + 自适应 α），冷启动用文本嵌入临时原型当天生效。
3. OOD 阈值 τ 在线分位数自适应，随分布漂移。
4. 降级：澄清最多 1 轮 → 回退 LLM → 记录难例（学习性降级）。

---

## 第 6 轮：规划与决策循环（低层 MPC + LLM 子目标）

**JEPA 专家**：低层 MPC：K=64 候选动作 × H=16 步 rollout，MoE 预测器 + 集成方差截断，cost 打分选首动作。延迟：预测器单步 <1 ms，批量后 K×H=1024 次前向 ~10–30 ms。

**架构师**：长时程任务**用 LLM 显式生成子目标**（H5：分层规划暂缓，无自动概念涌现）。子目标 = 自然语言子步骤，LLM 一次性分解（"先查库存 → 再下单 → 最后发邮件"），每个子目标编码为 `g_sub∈ℝ^128`，低层 MPC 以 `g_sub` 为局部目标逐个执行。

**DSH 专家**：探索/利用用 UCB：`score = 预测代价 − κ·不确定度`，不确定度 = 预测器集成方差（第 3 轮已给）。失败检测三条信号：预测误差 E 持续超阈、cost 单调上升、达成子目标时状态潜与 `g_sub` 距离不减反增。

**质疑者（四问）**

> **Q1** LLM 子目标是自然语言，每次执行子目标都要**重新编码成 `g_sub`**？子目标切换时重编码的延迟和"子目标边界怎么检测"——你怎么知道上一个子目标做完了、该切下一个了？
> **Q2** MPC 的 rollout 里，**每步要不要重跑状态编码器**？如果每 rollout 步都要重编码状态（62M 编码器），那 K×H 次编码直接爆延迟；如果只跑 0.42M 预测器，那 rollout 里的"状态"是纯潜状态，**工具的副作用（真实环境反馈）在 rollout 里怎么体现**？
> **Q3** 不确定度来自 4 个 0.42M 预测器的集成方差，这么小的模型集成的方差**有信息量吗**？还是只是随机种子的噪声？拿噪声当探索信号会误导向。
> **Q4** "轨迹潜距离发散"在纯文本下怎么算？没有视觉，128 维状态潜的几何距离**语义上是什么**？距离大就是失败吗？

**回应与修正**

- 对 Q1：**子目标边界 = 状态潜与 `g_sub` 的距离低于阈值**（达成判据），而非靠 LLM 每次判断。子目标重编码只在**切换时刻**发生（每任务 2–5 次，非每步），一次 LLM 编码 50–150 ms，摊到整个任务可忽略。为消除歧义：LLM 分解子目标时**同时输出每个子目标的"完成判据"**（一个可检验的状态条件），判据编码进 `g_sub` 的达成检测器，避免"距离达标但实际没完成"。
- 对 Q2：**关键修正：rollout 是"潜空间闭环 + 工具模型近似"**。rollout 里**不重跑 62M 编码器**，只跑 0.42M 预测器（这是 MPC 便宜的根本）。工具的副作用靠一个**轻量"工具结果模型"**近似：每个工具一个 `result ≈ 状态潜增量` 的均值估计（从历史轨迹统计，或一个小 MLP），rollout 到"执行工具"这一步时，用工具结果模型把工具潜增量加到状态潜上，再继续预测。**诚实的代价**：工具结果模型是近似的，所以 rollout 对"工具真实返回值"是盲的——这就是为什么 MPC 只做短时程（H=16）、且每步真实执行后**滚动重规划**（拿真实反馈刷新状态潜）。这是 MPC 的标准 trade-off，不是缺陷。
- 对 Q3：**集成方差信息量靠"训练扰动"而非"随机种子"**。4 个预测器用**不同的数据子集/不同的专家 dropout 模式**训练，使它们的分歧反映"数据覆盖不均"，而非纯噪声。若 P1 实测方差无信息量，降级为**显式的不确定度估计**：预测器额外输出一个 `σ` 头（heteroscedastic），直接回归预测误差的期望方差。这是可落地的 Plan B。
- 对 Q4：**距离的语义 = 预测误差，不是几何距离**。纯文本下"轨迹发散"不靠 128 维欧氏距离的绝对阈值（几何意义弱），而靠**相对量**：当前状态潜相对"预期子目标轨迹"的**预测误差 E 是否单调上升**。即失败信号是"世界模型越来越预测不准"，而不是"状态离目标远"。这更符合 JEPA 的语义（E 是校准信号）。

**本轮结论**
1. 低层 MPC：K=64 × H=16，延迟 10–30 ms；rollout 只跑预测器（不重编码），工具副作用用"工具结果模型"近似 + 滚动重规划补偿。
2. 长时程 = LLM 显式子目标（含完成判据），子目标切换靠达成检测器。
3. 不确定度 = 集成方差（训练扰动式），Plan B 为显式 σ 头。
4. 失败信号 = 预测误差 E 单调上升（相对量），非几何距离绝对阈值。

---

## 第 7 轮：实时训练与在线适应性（敏捷更新）

**JEPA 专家**：在线更新分层，且**必须秒级**（D4 缺陷）：

- **秒级（无梯度，非反向传播）**：
  1. **上下文校准**：对状态编码器 L1/L2 的 LayerNorm 统计（μ, σ）做**指数滑动重估**，适配新领域的文本分布偏移。耗时 <1 ms，纯统计量更新。
  2. **在线原型更新**：字段原型库、目标原型库、槽位原型库的 EMA 更新 + 在线 k-means 一步。秒级。
  3. **快速权重（fast weights）**：对参数分类器的输出层做**在线岭回归**（局部二次近似，闭式解），或对 actor 头用 TTT 式快速权重——把"最近 N 步的梯度"压成一个快速权重加到输出层，秒级、可逆。
- **梯度级（异步，秒~分级）**：actor/critic/分类器 head 的小批量梯度更新，异步后台，不阻塞。
- **LoRA 级（分钟级，触发式）**：T2，世界动力学剧变时才触发，主干冻结。

**DSH 专家**：所有秒级/梯度级更新走 `OnlineTrainer` 后台进程，主循环永远读"当前一致快照"，原子切换，绝不等梯度。

**质疑者（四问）**

> **Q1** "秒级适应"拿 LayerNorm 重估 + 原型 EMA 糊弄——这些只能适应**分布偏移**（文本风格变了），不能适应**新的动力学规律**（新工具、新交互逻辑）。真正的"学会新东西"必须改权重（梯度），而梯度级是"秒~分级"，你的"秒级"是偷换概念。
> **Q2** 快速权重具体给算法，别甩 TTT/岭回归名词。岭回归的闭式解要解线性方程组，在几百维上算不算快？快速权重和主权重怎么叠加、怎么保证不互相污染？
> **Q3** 灾难性遗忘：EMA 目标编码器是 JEPA 那套（预测器 target），但**状态编码器是文本编码器，不是 JEPA 结构**，它没有 EMA teacher。文本编码器侧的遗忘靠什么？
> **Q4** 环境剧变要重训状态编码器（新领域），冻结的 60M 编码器怎么**不重启主循环**地热切换？热切换瞬间正在跑的 rollout 和正在写的原型库怎么办？数据一致性谁保证？

**回应与修正**

- 对 Q1：**承认"秒级"要精确分层，修正措辞**。区分两类适应：**A 类（分布漂移）**：上下文校准 + 原型更新，真·秒级，非梯度；**B 类（新动力学）**：必须梯度，但可以做到**近秒级**——因为要更新的只是 **head 层（actor/critic/分类器/投影，共 ~30M）**，小批量 32 条、单步梯度在 GPU 上 <100 ms，异步累积到"1 秒内完成 1 步有效更新"是可达的。所以诚实的表述是：**"A 类秒级、B 类秒~分级（head-only）"**。主干（编码器）的 B 类适应是分钟级（LoRA），这是结构上必须接受的边界，用"head 承载绝大部分新任务的可塑性、主干只承载世界动力学"来缓解——新工具/新交互大多是"选哪个动作、填哪个参数"的 head 层面变化，不是世界动力学变化。
- 对 Q2：**给岭回归的具体算法**。快速权重作用于**分类器输出层 W∈ℝ^{C×d}**（C=该工具槽位类别数，d=64）：维护 `A = Σ x_i x_iᵀ + λI`（d×d=64×64，极小）和 `B = Σ x_i y_iᵀ`（64×C）两个累计矩阵；新样本 `(x, y)` 到达时 `A += xxᵀ; B += xyᵀ`（O(d²)=4K flops，μs 级），预测时 `W_fast = A⁻¹B`（64×64 求逆，μs 级）。**这是在线岭回归的标准增量式，64 维下求逆便宜到可忽略**。叠加方式：`输出 = W_base·x + W_fast·x`，主权重 W_base 冻结不动，快速权重独立、可整体清零回滚，不污染。这本质是"局部记忆"，天然抗遗忘（不写主干）。
- 对 Q3：**文本编码器侧遗忘三件套**：① 编码器 L0 **冻结**（默认永不更新，遗忘为零）；② 可训练的 L1 聚合层 + 投影层用 **EMA 慢副本**做蒸馏（把旧表示"钉"住，CLA 思想）；③ 字段原型库的 EMA 更新本身就是"过去表示的 O(1) 压缩"。若 L0 需要 LoRA 微调（新领域），用 **LoRA 隔离 + 旧领域原型重放**（重放旧领域字段原型，防止 LoRA 把旧表示冲掉）。
- 对 Q4：**热切换 = 双缓冲 + 版本号 + 只读快照**。状态编码器权重、原型库、码本全部**版本化**（v1 就定了潜侧版本化，这里落实）。切换流程：新编码器在后台加载到**独立显存缓冲** → 预热（跑几条样本校校准）→ 主循环在**安全边界**（当前任务结束）原子交换指针 → 旧版本延迟回收（等正在读的 rollout 结束）。一致性：主循环对"编码器权重 + 原型库"做**快照读**（每个决策步开始时固定版本号，本步内不换），因此正在跑的 rollout 不受影响，下个决策步自然用新版本。这是标准的 MVCC（多版本并发控制），不需要发明新机制。

**本轮结论**
1. 秒级适应精确分两类：A 分布漂移（校准+原型，真秒级）/ B 新动力学（head-only 梯度，秒~分级）；主干适应是分钟级 LoRA，属结构边界。
2. 快速权重 = 增量岭回归（64×64 闭式，μs 级），主/快权重分离、可回滚、天然抗遗忘。
3. 文本编码器遗忘控制 = L0 冻结 + L1/投影 EMA 蒸馏 + 原型重放 + LoRA 隔离。
4. 热切换 = 版本化 + 双缓冲 + 快照读（MVCC），不重启、不打断 rollout。

---

## 第 8 轮：离线巩固与整体可行性（渐进式路线）

**JEPA 专家**：离线巩固四件事（同 v1）：① 原型蒸馏（聚类 outcome 潜 → 更新原型库）；② 码本更新（聚类动作潜，淘汰冷门、合并近邻、吸收新工具）；③ 目标映射器重训（新增 (goal,outcome) 对比）；④ 预测器精修（回放重训，抑制 rollout 漂移）。

**架构师**：**渐进式路线是本次最大修正（D6）**。JEPA 的决策权是逐步授予的，不是一步到位：

- **P0 预测辅助**：LLM 生成 top-K 候选动作（一次 decode），JEPA 对候选做后果预测排序，选最优。JEPA 不独立采样，LLM 仍是主体。
- **P1 世界模型**：验证 JEPA 的预测/排序准确率。
- **P2 半自主**：JEPA 采样动作，但置信门控——`confidence < τ` 时交还 LLM。
- **P3 自主**：JEPA 独立决策，LLM 仅做子目标分解 + 自由文本参数 + 最终兜底。

**DSH 专家**：提升决策权必须**数据驱动**，给可量化触发指标，且全程 A/B 影子对照。

**质疑者（四问）**

> **Q1** P0"预测辅助"的排序准确率要多少才有价值？如果 JEPA 排序比 LLM 默认顺序还差，P0 就是**负优化**（白花排序延迟，不涨成功率）。给止损线。
> **Q2** "验证通过后提升决策权"太模糊。给**可量化的升级触发指标**，别用"验证通过"这种话术。
> **Q3** 平滑过渡怎么防"拔掉 LLM 后成功率断崖"？影子模式 + 置信门控 + 降级，这套有没有具体的 A/B 评估协议？
> **Q4** 整体风险：如果 P0 就证明 JEPA 排序不优于 LLM，整个项目是否止损？止损线和替代出路是什么？

**回应与修正**

- 对 Q1：**P0 价值判据 = 排序收益（ranking lift）**。指标：`P@1 提升 = JEPA排序后首选的候选 vs LLM 默认首选的候选，哪个真实成功率更高`。**止损线**：连续两轮评测 P@1 无提升（甚至负提升）→ 判定"当前状态编码/预测器不足以支撑排序"，转入**纯世界模型辅助形态**（JEPA 只做异常检测/成本预估，不参与排序，回到"预测辅助的辅助"），而非强推。这是明确的止损出口。
- 对 Q2：**升级触发指标（定量）**：从 P0 升 P2 需同时满足——① 排序 P@1 提升 ≥ +5% 且持续 ≥100 条评测轨迹；② JEPA 排序的 top-1 候选成功率 ≥ 90% × LLM 基线；③ 预测误差 E 的滑动均值低于预设校准线。从 P2 升 P3 需——④ 置信门控触发率（JEPA 主动交还 LLM 的比例）连续下降且 < 20%；⑤ 影子模式下 JEPA 独立决策的成功率 ≥ LLM 基线。**三条/五条全满足才升级，缺一不可**。
- 对 Q3：**A/B 协议**：全程跑"影子 JEPA"（不干预、只记录"如果我来会怎么选"），与线上 LLM 决策做**配对比较**（同任务、同初始状态）。升级前用影子数据做**离线回放评估**（在历史轨迹上重放 JEPA 决策，看反事实成功率）。升级用**灰度**：10% 流量走 JEPA（置信门控内）→ 30% → 100%，每档观察成功率，掉 3% 即回滚。这套协议标准、可执行。
- 对 Q4：**止损线明确**：P0 连续两轮 P@1 无提升 → 降级为"纯世界模型辅助"（异常检测 + 成本预估），这仍有独立价值（LLM Agent 的常见痛点是幻觉动作 + 成本失控，JEPA 的预测误差能实时预警"这步动作后果异常"）。**即使 JEPA 永远不做决策，作为"后果预测器/风险哨兵"也是有用的**。所以最坏情况不是"项目白做"，而是"定位从决策器退化为哨兵"——这是诚实的风险下限。

**本轮结论**
1. P0 判据 = 排序 P@1 提升；止损线 = 连续两轮无提升 → 降级为"世界模型哨兵"（仍有用）。
2. 升级触发 = 5 条定量指标（P@1 +5%、top-1 ≥90% 基线、E 达标、交还率 <20%、影子成功率 ≥ 基线），全满足才升级。
3. 平滑过渡 = 影子对照 + 离线回放评估 + 灰度（10→30→100%，掉 3% 回滚）。
4. 风险下限 = JEPA 退化为"后果预测/风险哨兵"，非白做。

---

# 第二部分：技术方案文档

---

## 1. 总体架构（文字描述）

### 1.1 插件清单

| 插件 | 类型 | 职责 |
|---|---|---|
| `JEPA-Loop` | 新增（Agent Loop） | 潜空间决策循环；P0 为"排序器"，P2+ 为"采样器"；接口 `tick(goal_latent, state_latent, candidates) -> (action, confidence)` |
| `StateEncoder` | 新增 | 稀疏层级编码器：JSON 观察 → 字段级 → 状态潜 `s∈ℝ^128` |
| `GoalMapper` | 新增 | 目标（文本）→ 目标潜 `g∈ℝ^128` + OOD 置信度 |
| `ActionCodebook` | 新增 | 动作码本（簇层 + 工具层），工具码绑定 |
| `ParamDecoder` | 新增 | 模板填充 + 分类器（最近邻原型）参数解码，LLM 仅自由文本 |
| `WorldModel`（MoE Predictor） | 新增 | 稀疏专家混合预测器：`Δŝ = g_φ(s,a)` |
| `Planner` | 新增 | 低层 MPC + UCB（集成不确定度）+ 失败检测 |
| `Critic`（Cost） | 新增 | 状态/轨迹代价评估 |
| `OnlineTrainer` | 新增 | 秒级适应（校准/原型/快速权重）+ 异步梯度更新 + 遗忘控制 |
| `OfflineConsolidator` | 新增 | 原型蒸馏 / 码本更新 / 目标映射器重训 |
| `LLM` 插件 | 修改 | 定位：候选生成（P0）+ 子目标分解 + 自由文本参数 + 最终兜底 |
| `TrajectoryLog` 插件 | 修改 | 每条消息追加 `latent` sidecar 字段 |

### 1.2 数据流（纯文本/JSON，一个完整任务周期）

```
用户目标(文本)
   │
   ▼
GoalMapper: LLM编码 → 投影 → 目标原型kNN插值校正 → g∈ℝ^128, conf
   │ conf<τ(t) → 澄清(≤1轮) → 仍OOD → 回退LLM全程规划
   ▼
当前观察(JSON) ──► StateEncoder(增量: 仅变化字段重编码 + 原型缓存) ──► s∈ℝ^128
   │
   ▼
┌────────── JEPA-Loop 决策（P0 预测辅助）──────────┐
│ LLM 一次decode生成 top-K 候选动作(含参数)          │
│ Planner: 对每个候选 rollout H=16 步(MoE预测器)     │
│ Critic 打分 + UCB(集成不确定度)                     │
│ → 排序选最优候选 → (action, confidence)            │
└───────────────────────────────────────────────────┘
   │ (P2+: 若 confidence<τ 交还LLM; P3+: JEPA独立采样)
   ▼
ParamDecoder: action潜→(最近邻)工具码→模板/分类器填参数
   │ 仅 free-text 参数 → LLM 填空
   ▼
DSH 执行插件 → 新JSON观察
   │
   ├─► StateEncoder → s_{t+1}; 预测误差 E=‖ŝ_{t+1}−s_{t+1}‖
   │      ├─ 秒级适应: 上下文校准 + 原型EMA + 快速权重(OnlineTrainer, 异步)
   │      ├─ E持续高 → 低层重编码 / 异步head梯度 / 触发LoRA
   │      └─ E单调升 → 失败检测 → 重规划 / 交还LLM
   └─► 轨迹日志追加 (goal, s_t, action, s_{t+1}, outcome) + latent sidecar
```

### 1.3 双总线（修正版）

- **单一文本总线**：DSH 插件生态的消息主通道，兼容不变。
- **潜空间 sidecar**：文本总线上每条消息的伴随潜表示，**惰性计算**（仅 JEPA-Loop 消费的消息才编码），**唯一写入方**（StateEncoder），**版本化 + 快照读**（MVCC）。不再是一条物理独立的第二总线。

---

## 2. 核心模块设计

### 2.1 稀疏层级状态编码器 `StateEncoder`

| 层 | 输入 | 结构 | 输出维度 | 参数量 | 更新 |
|---|---|---|---|---|---|
| L0 文本编码 | JSON 字段文本 | 冻结 ~60M 编码器（MiniLM/BGE-small 量级） | token 表示 | ~60M | 冻结（LoRA 例外） |
| L1 字段聚合 | 字段 token 表示 | 2 层 Transformer + 门控（输出 mean+var） | `f_i∈ℝ^256` | ~3M | EMA 蒸馏 |
| L2 状态池化 | 字段向量集合 | 2 层 MLP 池化 | `s∈ℝ^128` | ~2M | 在线/离线 |

- **稀疏激活机制**（D1 修正的核心）：
  1. **增量编码**：字段编码结果缓存于字段原型库，仅**语义变化**（余弦 >ε）的字段重编码。
  2. **高层优先**：默认只用 L2 + 变化字段的 L1 缓存（激活 ~5M）。
  3. **低层触发**：三个信号（全局预测误差 E 超阈 / L1 门控注意力熵高 / L1 残差方差 var 大）任一满足 → 触发 L0 重编码（仅变化字段，激活 ~17M）。
- **输入/输出**：输入 = JSON 序列化文本；输出 = `(s∈ℝ^128, var∈ℝ^128)`（var 为状态不确定度，供低层触发与规划）。
- **训练**：L0 冻结；L1/L2 用 LLM-JEPA 式目标训练（预测下一状态潜，stop-grad target）；投影层数据来自轨迹日志观察对。
- **参数量/激活量**：总 ~65M；默认激活 ~5M，触发低层 ~17M（稀疏比 8%–26%）。

### 2.2 稀疏专家混合预测器 `WorldModel`

- **结构**：输入 `(s∈128, a∈64)` 拼接（192）→ 路由（线性+softmax，top-2，噪声注入）→ 2 个专家 MLP（192→256→128）→ 加权输出 `Δŝ∈128`（残差）。
- **规格**：专家数 E=8，每专家 ~50K；路由 ~16K；**总 ~0.42M，激活 ~100K**（top-2）。
- **路由设计**：auxiliary load-balancing loss + 噪声注入防坍缩；**轨迹内路由一致性正则**（相邻 rollout 步选同专家）防抖动。
- **训练**：`L = ‖Δŝ + s_t − sg(s_{t+1})‖²`，target 用 EMA 目标编码器（stop-grad）；集成 4 个预测器（不同数据子集/dropout 模式），输出均值 + 方差。
- **漂移控制**：残差模式 + EMA target + 集成方差截断（rollout 中方差超阈即截断该分支）。
- **升级路径**：专家 8→32、隐藏 256→512、输入加 4 步历史（若 P1 预测能力不足）。

### 2.3 动作码本与参数解码器

- **码本**：两层。簇层 `C∈ℝ^{32×64}`；工具层 `T∈ℝ^{K×64}`（K≤512）。工具码 = LLM 编码 `(name+docstring+schema)` 投影到 ℝ^64。
- **动作潜映射**：`a∈ℝ^64 → code = argmin_c ‖proj(a) − T[c]‖²`。
- **参数解码（模板 + 分类器优先，D3 修正）**：
  1. schema 自动生成模板骨架与槽位类型。
  2. `enum/boolean` → 槽位原型库最近邻分类；`number` → 轻量回归；`structured sub-object` → 递归模板。
  3. `format: free-text` → **仅此类**调用 LLM 填空（槽位已定位）。
- **分类器**：共享"槽位选择注意力"（决定填哪个槽）+ 槽位原型库（最近邻，零训练扩展）。
- **参数量**：注意力 ~5M + 槽位原型库（每槽位枚举值一个 64 维原型，随工具数，~几 M）。

### 2.4 目标映射器 `GoalMapper`

- **输入**：目标文本；**输出**：`g∈ℝ^128` + `conf`。
- **流程**：LLM 编码 → 投影（2 层 MLP）→ 目标原型 kNN 插值校正：`g = α·投影 + (1−α)·kNN`，α 由最近原型距离自适应。
- **训练**：InfoNCE + **困难负例挖掘**（同批负例 + 终态近/语义远困难负例 + 跨簇近投影负例）。
- **OOD 检测**：`conf = −min_dist(g, 目标原型库)`；阈值 `τ(t)` = 历史 conf 的 10% 分位（EMA 在线自适应）。
- **冷启动**：LLM 影子规划 bootstrap；文本嵌入直接当临时原型，当天生效。
- **参数量**：投影层 ~2M。

### 2.5 规划器 `Planner`

- **低层 MPC**：K=64 候选 × H=16 步；rollout 只跑 MoE 预测器（不重编码），工具副作用用"工具结果模型"（每工具的状态潜增量均值估计）近似；滚动重规划（每步真实执行后刷新）。
- **UCB**：`score = 预测代价 − κ·不确定度`，不确定度 = 集成方差（Plan B：显式 σ 头）。
- **长时程**：LLM 显式子目标（含完成判据）→ 编码为 `g_sub` → 低层 MPC 逐个执行（H5：无自动概念涌现）。
- **失败检测**：E 单调上升 / cost 上升 / 达成判据不满足 → 重规划或交还 LLM。

---

## 3. 工具调用机制

### 3.1 码本构建与更新

1. 遍历 DSH 插件，LLM 编码 `(name, docstring, schema)` → 工具嵌入 → 投影 ℝ^64。
2. 离线聚类成 32 簇，簇内建索引。
3. 新插件注册 → 增量加码点 + 重建局部索引 + schema 推导模板/分类器。

### 3.2 动作潜 → 工具调用映射算法

```
a = actor(s, g)                       # ∈ℝ^64 (P0: 候选由LLM给出, actor仅排序)
code = argmin_c ‖proj(a) − T[c]‖²
tool = codebook.lookup(code)
slot_attention = attn(a, schema)      # 决定填哪些槽
params = template_fill(tool, slot_attention, s)   # 模板+分类器
if any(slot.format == free-text): params[slot] = LLM_fill(...)
```

### 3.3 参数生成延迟估算（以 HTTP 请求为例）

| 参数 | 类型 | 方式 | 延迟 |
|---|---|---|---|
| `method` | enum | 原型最近邻 | <1 ms |
| `url` | 结构化 | 模板从状态取值 | <1 ms |
| `headers` | kv | 模板拷贝 | <1 ms |
| `body` | free-text/JSON | LLM（仅此） | 50–150 ms |

常规调用 <5 ms；含自由文本 +50–150 ms。**大多数工具调用零 LLM**。

---

## 4. 实时训练与适应性机制

### 4.1 在线更新策略

| 组件 | 更新方式 | 频率 | 延迟级别 |
|---|---|---|---|
| 状态编码器 L0 | 冻结（LoRA 例外） | — | 分钟级（LoRA） |
| L1/L2 | EMA 蒸馏 + 异步梯度 | 每 N=32 转移 | 秒~分级 |
| 预测器 | 冻结（回放重训） | 离线 | — |
| actor/critic/分类器/投影 head | 异步小批量梯度 | 每 N=32 | 秒~分级 |
| 分类器输出层快速权重 | 增量岭回归（闭式） | 每样本 | **μs~秒级** |
| LayerNorm 统计 | EMA 重估（校准） | 每样本 | **<1 ms** |
| 字段/目标/槽位原型库 | 在线 EMA + k-means 一步 | 每样本/每轨迹 | **秒级** |

### 4.2 秒级适应算法（D4 修正）

**A 类（分布漂移，真·秒级，非梯度）**：
1. **上下文校准**：`μ ← (1−β)μ + β·x; σ² ← (1−β)σ² + β·(x−μ)²`，重估 L1/L2 LayerNorm 统计，适配新领域文本分布。β=0.01，<1 ms。
2. **在线原型更新**：字段/目标/槽位原型的 EMA 更新 + 在线 k-means 一步（新向量归入最近原型并 EMA 更新之，或超距则新建槽）。

**B 类（新动力学，head-only 梯度，秒~分级）**：
3. **快速权重（增量岭回归）**：维护 `A=Σxxᵀ+λI`（64×64）、`B=Σxyᵀ`；新样本增量更新；预测时 `W_fast=A⁻¹B`（μs 级求逆）。作用于分类器输出层，主权重冻结、快速权重独立可回滚。

**C 类（世界动力学剧变，分钟级）**：LoRA 残差微调主干，触发式。

### 4.3 遗忘控制

1. 状态编码器 L0 冻结（遗忘为零）。
2. L1/投影层 EMA 慢副本蒸馏（CLA 思想，钉住旧表示）。
3. 原型重放（旧领域字段原型重放）。
4. 快速权重/head 与主干隔离（不写主干）。
5. LoRA 隔离（残差独立、可回滚）。

### 4.4 在线学习开销与延迟预算

| 项 | 延迟 | 是否阻塞主循环 |
|---|---|---|
| 上下文校准 | <1 ms | 否（异步） |
| 快速权重更新 | μs 级 | 否 |
| 原型 EMA | <1 ms | 否 |
| head 梯度 1 步 | <100 ms | 否（异步后台） |
| LoRA 微调 | 1–3 s/batch | 否（异步，触发式） |
| **主循环（状态编码 + 规划 + 解码）** | **~20–60 ms 默认；~200 ms 触发 LLM/全量重编码** | 是 |

**单步主循环延迟 ~20–60 ms ≪ 1.5s（H3）；显存峰值 <6.5 GB ≪ 20 GB（H2）**。

---

## 5. 离线训练与巩固流程

1. **数据采集**：轨迹日志（含 latent sidecar）自动回填；清洗（丢 E 异常噪声、去重、任务均衡）。
2. **原型蒸馏**：聚类 outcome 潜（k-means，上限 1024，余弦 >0.7 合并）→ 更新目标/结果原型库。
3. **码本更新**：动作潜再聚类，淘汰冷门、合并近邻、吸收新工具。
4. **目标映射器重训**：新增 `(goal, outcome)` 困难负例对比重训。
5. **预测器精修**：回放缓冲重训，抑制 rollout 漂移（Clin-JEPA 思想）。
6. **版本协调**：离线产出新版本快照 → 安全边界原子切换 → 失败回滚（MVCC，与在线更新"离线为主、在线为补"）。

---

## 6. 渐进式实施路线图（MVP 优先）

| 阶段 | 交付 | 验证指标（升级/止损门槛） |
|---|---|---|
| **P0 预测辅助**（1–2 周） | 双总线 sidecar + StateEncoder + MoE 预测器 + 排序闭环；LLM 生成 top-K 候选，JEPA 排序 | **P@1 提升**；止损：连续两轮无提升 → 降级"世界模型哨兵" |
| **P1 世界模型**（2–3 周） | 预测器精度 + rollout 稳定性（A1 门槛） | 预测误差 E 低于随机基线；rollout 16 步漂移可控 |
| **P2 半自主**（3–4 周） | JEPA 采样 + 置信门控 + 交还 LLM | 升级三条件：P@1 ≥+5%、top-1 ≥90%×基线、E 达标 |
| **P3 自主**（4–6 周） | JEPA 独立决策；LLM 仅子目标+自由文本+兜底 | 升级五条件：+交还率 <20%、影子成功率 ≥ 基线 |
| **P4 巩固**（6–8 周） | 秒级适应全量 + 离线巩固闭环 + LoRA 触发 | 新工具秒~分级适应成功率；遗忘率 BWT 可控 |

**提升决策权的方式**：全程影子对照 + 离线回放评估 + 灰度（10%→30%→100%，成功率掉 3% 即回滚）。**JEPA 的决策权从"排序"→"门控采样"→"独立采样"逐级授予，LLM 永不整体移除（保留子目标/自由文本/兜底）**。

---

## 7. 可行性分析与风险

### 7.1 关键风险与缓解

| 风险 | 缓解 |
|---|---|
| 潜空间 rollout 漂移（A1） | 残差模式 + EMA target + 集成方差截断 + 短时程滚动重规划 |
| 稀疏预测器预测能力不足 | 升级路径（专家 8→32、加历史）；或 dense 单预测器 |
| 集成方差无信息量 | Plan B：显式 σ 头（heteroscedastic） |
| 跨模态（若引入视觉） | 视觉仅作可选扩展；纯文本为主，视觉走冻结 V-JEPA 2 骨干 + 对齐层，与文本潜拼接（A2） |
| 自由文本参数占比过高 | 该类工具由 LLM 主理（正确分工，不硬扛） |
| 升级后成功率断崖 | 灰度 + 掉 3% 回滚 + 置信门控 + 学习性降级 |

### 7.2 与纯 LLM 方案对比预期

| 维度 | 纯 LLM Agent | JEPA×DSH v2 |
|---|---|---|
| 零样本新任务 | 强 | 弱→中（回退 LLM） |
| 长时程规划一致性 | 中 | 中→强（LLM 子目标 + 低层 MPC） |
| 工具参数生成延迟 | 高（每次 LLM JSON） | **低（模板/分类器，多数零 LLM）** |
| 单步推理延迟 | ~1 LLM decode（0.3–1s） | **~20–60 ms（决策）+ LLM 仅在子目标/自由文本** |
| 环境动态建模 | 弱 | 中（潜空间世界模型） |
| 幻觉动作风险 | 高 | **低（预测误差实时预警）** |
| 推理成本 | 高 | 低（MPC 走 0.42M 预测器） |

**定位重申**：JEPA 是"**世界模型增强 + 决策降本**"，不是替代 LLM。最坏情况退化为"后果预测/风险哨兵"（仍有独立价值），最好情况成为"低成本、可自适应的决策核心 + LLM 兜底"。

---

## 附录 A：假设清单

A1 稀疏预测器潜空间 rollout 稳定性（P1 准入门槛，失败则缩短 H 或降级 dense）。
A2 视觉分支为可选二期（纯文本为主，迁移路径已给）。
A3 字段缓存命中率 70–90%、自由文本参数占比 <30% 为经验预期，需 P0/P1 实测校正。

## 附录 B：关键参考文献

- LeCun (2022). *A Path Towards Autonomous Machine Intelligence*. OpenReview.
- Assran et al. (2023). *I-JEPA*. CVPR 2023, arXiv:2301.08243.
- Bardes et al. (2024). *V-JEPA*. arXiv:2404.08471.
- Assran et al. (2025). *V-JEPA 2*. arXiv:2506.09985.
- Huang, LeCun, Balestriero (2025). *LLM-JEPA*. arXiv:2509.14252.（纯语言 JEPA 关键依据）
- Meta AI (2025). *VL-JEPA*. arXiv:2512.10942.
- Yang et al. (2026). *Clin-JEPA*. arXiv:2605.10840.
- Fini et al. (2022). *CaSSLe*. CVPR 2022.
- Cignoni et al. (2025). *CLA*. CoLLAs 2025, arXiv:2507.10434.
- Shazeer et al. (2017). *Outrageously Large Neural Networks: The Sparsely-Gated MoE*（路由/负载均衡依据）.
