# JEPA 实践内核（JPI-1）：以成熟方案补齐的认知架构设计

> 日期：2026-08-24 ｜ 原则：**每个环节只采用有公开实现/论文支撑的成熟方案**，不发明新东西；空白处（元认知）用已验证组件的组合。
> 依据：V-JEPA 2、LeWorldModel、EB-JEPA、LLM-JEPA、Micro-World Models、Curiosity-Critic、AdaJEPA、SIGReg/LeJEPA、stable-worldmodel、Remember-to-be-Curious。

---

## 0. 一句话

**JPI-1 是一个"预测器 + 记忆 + 目标 + 两级能量"的闭环认知体，全部组件都有 2025-2026 公开实现可抄，唯一需要原创组合的是把它们接起来的调度逻辑。**

---

## 1. 总体架构（五层 + 两贯穿）

```
┌─ 元认知 Configurator（原创组合，非原创组件）─────────────┐
│  目标生成: 记忆采样/插值/随机  能力暴露: 阶段化调制         │
│  自指: Self-model 预测自身状态   调度: 能量趋势驱动切换      │
└───────────────────────────────────────────────────────┘
┌─ 符号层（LLM-JEPA / VL-JEPA 路线）───────────────────────┐
│  语言作为查询接口: 嵌入空间视图预测, 不逐token生成          │
└───────────────────────────────────────────────────────┘
┌─ JEPA 世界模型（LeWorldModel / EB-JEPA 路线）────────────┐
│  编码器 fθ: 像素/传感器 → s∈ℝ^256（VICReg 或 SIGReg 防坍缩）│
│  预测器 P: (s, a, δ) → Δŝ 残差预测（集成 + 可信度传播）      │
│  规划器: CEM K×H 潜空间搜索（无解码）                     │
└───────────────────────────────────────────────────────┘
┌─ 感知层（V-JEPA 2 编码器 / ConvEncoder 路线）─────────────┐
│  可学习编码, 按可预测性丢弃细节                            │
└───────────────────────────────────────────────────────┘
┌─ 身体层（V-JEPA 2-AC / Playground 路线）─────────────────┐
│  可交互环境 + 持久世界模型 + 情景记忆（防健忘式探索）        │
└───────────────────────────────────────────────────────┘
贯穿: 两级能量（E1 预测误差 + E2 价值涌现）
     时间分层（秒/分/时/天, H-JEPA 堆叠）
```

---

## 2. 每环节成熟方案选型表

| 环节 | 成熟方案（来源） | 参数/规模 | 关键机制 | 取舍 |
|---|---|---|---|---|
| 感知编码 fθ | V-JEPA 2 编码器（Meta 开源权重）；轻量替代 LeWorldModel 端到端（2603.19312） | ViT-L/16 300M 或 LeWM ~15M | 按可预测性丢弃细节；SIGReg 高斯约束防坍缩 | 大权重直接复用；小模型自训 |
| 世界模型 P | LeWorldModel 两 loss 端到端；EB-JEPA 单 GPU 库（2602.03604） | 15M~300M | 残差预测 Δŝ；VICReg/SIGReg；集成输出 mean+var | EB-JEPA 有现成 ac_video_jepa 规划示例 |
| 规划器 | stable-worldmodel CEM/iCEM/MPPI（2605.21800）；V-JEPA 2-AC 规划循环 | 逻辑模块 | 潜空间 CEM 搜索，无解码；可信度截断 | 直接复用求解器实现 |
| 时间分层 | Micro-World Models H-JEPA（2026.06, Zenodo 开源） | 双尺度 tap | 浅层短期 + 深层宏观事件预测；VICReg 防坍缩无 EMA | 首个可复现实现 |
| 符号层 | LLM-JEPA（2509.14252）；VL-JEPA（2026） | 附加 JEPA 损失于 LLM | 嵌入空间视图预测；语言作查询 | LLM 是工具非核心 |
| 记忆 | Remember-to-be-Curious 持久模型+情景记忆（2605.22814）；AdaJEPA recent-5 回放 | 3DGS 或矩阵 + 线性注意力 | 持久性 + 情景上下文 = 好奇心前提 | 防健忘式探索 |
| 在线适应 | AdaJEPA（2606.32026） | 1 步梯度，最后层，lr 5e-4/enc 1e-5 | 测试时自适应闭环 | 直接抄配方 |
| 两级能量 | Curiosity-Critic（2604.18701） | 批评者单标量 | E1=预测误差；E2=累积改进−基线（分离认识论/偶然误差） | 价值涌现的工程化 |
| 目标生成 | 记忆采样/插值/随机（LeCun 蓝图 + DCA InfoDrives 经验） | 逻辑模块 | 任何状态可成目标 | 需坚持性预算 |
| 元认知调度 | Configurator 蓝图（LeCun 2022）+ AdaJEPA 闭环 | 逻辑模块 | 能力暴露阶段化；能量趋势切换 | **唯一原创组合点** |

---

## 3. 核心循环（单 tick）

```
观测 o_t → fθ(o_t) → s_t ∈ ℝ^256
  → 记忆检索: familiarity = −log‖s_t − 最近原型‖
  → 目标选择: g = 记忆点/采样/插值（Configurator 调制）
  → 规划: CEM K=64 × H=16 rollout 通过 P(s,a,δ)
       ├─ Veto: 撞近危险原型 → 该候选 +惩罚（硬编码锚）
       └─ 选择 min ΣE 的动作序列, 执行第一步
  → 观测 o_{t+1} → s_{t+1} → E1 = ‖Δŝ − (s_{t+1}−s_t)‖²
       ├─ E1 > τ → 写入记忆（surprise 门控）
       ├─ 1 步梯度更新 P（AdaJEPA 配方）
       ├─ E2 更新: 累积预测误差改进（Curiosity-Critic 基线）
       └─ Self-model 预测自身状态（健康锚检查）
  → 下一 tick
```

**两级能量的分工**：E1 驱动训练（预测误差回传）与惊讶检测（触发记忆/学习）；E2 驱动行动选择（价值 = 哪个动作能带来最多"可学习的预测改进"）。E2 由 E1 的长程统计涌现，非外部奖励——这正是"软锚"路线：只硬编码生存/危险基线，价值内容由实践塑造。

---

## 4. 已知风险与边界（诚实声明）

| 风险 | 来源证据 | 缓解 |
|---|---|---|
| 纯 E1 驱动会"预测饱和死寂" | DCA 60k tick CP 钳位；Remember-to-be-Curious 健忘探索 | E2 持续产生行动理由；持久世界模型防循环 |
| LLM-JEPA 需配对视图数据 | 2509.14252 局限 | 符号层先用语言作查询接口，符号涌现走原型聚类 |
| H-JEPA 只在合成环境验证 | Micro-World Models 局限 | 先复现三体碰撞，再扩真实场景 |
| Veto 无价值语义 | DCA v2 校准 | 硬编码危险原型 + 健康基线（LeCun intrinsic cost 路线） |
| 统一空间 vs 多空间 | VL-JEPA 需对齐模块 | 接受"多空间+可逆映射"，不强行单一空间 |

---

## 5. 落地路径（P0 验证什么）

| 阶段 | 内容 | 验证指标 |
|---|---|---|
| P0 最小闭环 | 5×5 网格世界：编码器 + 残差预测 + 两级能量 + 记忆 + CEM | E1 收敛、E2 驱动探索、目标切换出现、记忆增长 |
| P1 时间分层 | 加 Micro-World 双尺度 tap | 宏观事件预测准确率 |
| P2 符号接口 | 接 LLM-JEPA 目标 | 语言查询→嵌入跟踪 |
| P3 真实身体 | Playground 768×768 或 V-JEPA 2-AC 权重 | 零样本规划成功率 |
